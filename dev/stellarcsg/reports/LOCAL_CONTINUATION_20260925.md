# Local continuation receipt — 2026-09-25

## Isolation and resource gate

- Actual repository: `openmc-stellarcsg`; continuation worktree/branch: `stellarcsg-local-continuation-20260925` / `JS/stellarcsg-local-continuation-20260925`.
- Starting production commit: `c67b68fdaf7be2049308db7da449f14a25123847`. Evidence-only control: `c92ae5a6c497650f0cbf0218a5ec00ae59817d00` in a separate clean worktree.
- Windows RAM: 33,457,440 KiB visible, 3,668,704 KiB free at audit. C: 89,636,298,752 B free of 998,965,768,192 B; D: 506,377,859,072 B free of 2,000,397,791,232 B. C: is system and target; D: is local data drive.
- Largest resident processes at audit: Memory Compression 2.20 GB, ChatGPT 1.13 GB, Chrome 0.98 GB, Codex 0.67 GB, Explorer 0.67 GB; further Chrome/ChatGPT/OneDrive processes were active.
- WSL `OpenMC-Dev-D`: MemTotal 24,609,408 KiB, MemAvailable 23,799,396 KiB after startup; root filesystem 1,021,385,912,320 B available; C: and D: mount space agreed with Windows. No significant WSL compute process appeared in the top RSS list.
- Existing tools: Windows Git 2.55.0, Python 3.12.10, CMake 4.4.0; WSL Git 2.47.3, GCC 14.2.0, CMake 3.31.6, Python 3.13.5. Existing `/opt/openmc-venv` is 606 MB; WSL pip cache is 152 MB. Windows pip cache and `.cache` exist; no Windows `.conda` or `C:/ProgramData/miniconda3` was found. Existing `OPENMC_CROSS_SECTIONS` points to a present local `cross_sections.xml`.
- Existing source/build checkouts are present in the StellarCGS directory, including the actual OpenMC repository, root-repair evidence and benchmark arena; `openmc-stellarcsg/build` and `stellarcsg-root-repair-07/build` already exist. They are controls, not write targets.
- Acquisition decision: no external software or data needed for the present patch/test milestone; planned download/install bytes: 0. The isolated local worktree uses already-local Git objects on C:. Any later build must use the existing compiler/HDF5/toolchain, be bounded by free RAM/disk, and record its exact output target and rollback before starting. The worktree and its branch are retained for review, not automatically removed.
- No SSH, remote jobs, protected-ref changes, or publication occurred in this task.

### Standalone build preflight

- The pre-build refresh showed 3,111,672 KiB Windows RAM free, 89,178,779,648 B free on C: and 506,235,297,792 B on D:. The build target `build/local-cont-20260925` did not exist. The earlier significant-process inventory still applies; memory is tight, so compilation is limited to one job.
- WSL already has HDF5 1.14.5, Ninja, C++, linker, taskset, and `/opt/openmc-venv`; no package acquisition is needed. The standalone `dev/stellarcsg/CMakeLists.txt` has no detected FetchContent/ExternalProject/download/pip-install hooks.
- Planned new software acquisition bytes: 0. Expected local build output budget: at most 1 GiB on C:, within the observed 89 GB free. The target is this continuation worktree's `build/local-cont-20260925`. Rollback, if needed after retaining receipts, is removal of that exact build directory; no shared build or environment is modified.

## Portable benchmark runner recovery

- Chat artifact `benchmark-portability.patch` was downloaded through the saved ordinary ChatGPT conversation after the local resource gate. Its SHA-256 is `a86eb95596b462b60eb062b2889245e69619ee6e98953498bd6bfd669ba8dc8d`, matching the review. `git apply --check` succeeded against this full local checkout.
- The additive patch creates `dev/stellarcsg/experiments/benchmark_portability/` only. It does not change the production solver or adapter.
- Test command: `PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 /opt/openmc-venv/bin/python dev/stellarcsg/experiments/benchmark_portability/test_portability.py` from this worktree inside WSL `OpenMC-Dev-D`.
- Result: 76 tests passed in 10.535 s, exit 0. These use analytic/mock fixtures and validate receipt mechanics; they are not native WISTELL-D or ZTorus measurements. The new runner remains opt-in.

## Outstanding qualification

Native ZTorus and recovered-old have a same-session unlike-bank sentinel below; no corrected candidate has been measured. The root, floating enclosure, adapter, physical fidelity, period transport and source interface gates remain open. No benchmark or physics pass is inferred from the 76 runner tests.

## Recovered-old and native ZTorus sentinel

- Built the unmodified recovered-old standalone library and benchmark from `c67b68fdaf7be2049308db7da449f14a25123847` in `build/local-cont-20260925` with WSL GCC 14.2, HDF5 1.14.5, CMake/Ninja Release, one build job, benchmarks and tests ON, campaigns/tools OFF. No dependency was downloaded or installed. Build exit 0; CTest 2/2 pass.
- The local native control executable at `D:/codex-verification/stellarcsg-20260913-03/native-torus-query-control-02` has the expected SHA-256 `9ec5ecf208507fd0a39e87e79a3cc36cbed236ed9c2ea63361e1b5554d34486d`. Its source calls `openmc::torus_distance`; `nm` shows that symbol unresolved in the executable, and `ldd` resolves `libopenmc.so` from the existing local native OpenMC build. The new runner checks that loader binding before timing.
- An initial command used `/coils/coil_031`, which does not exist in the qualified HDF5. It exited 1 with HDF5 group-not-found diagnostics; no timing was accepted. The file contains 48 groups, `/coils/coil_1000` through `/coils/coil_1047`; the corrected query uses `/coils/coil_1031`. A deliberate nonexistent-dataset check then confirmed the revised coordinator exits 1, writes `state=FAIL` with child exit 1, and omits metrics. Its receipt is `local-cont-20260925/failure-invalid-dataset.json`.
- Final same-session interleaved sentinel receipt: `local-cont-20260925/old-native-sentinel-03.json`. Seven one-core, one-thread repetitions each; all children exited 0. Recovered-old distance median of complete bank means: **4,832 ns/query** (range 4,756.1–4,991.5); native ZTorus median: **175.06546875 ns/query** (range 171.0868765625–181.43890625). The arithmetic old/native sentinel cost ratio is **27.6011**, explicitly **not** a matched geometry or bank speed comparison. Earlier successful runs are preserved as `old-native-sentinel.json` and `old-native-sentinel-02.json`.
- The old coil executable timed 1,000 unique direction rays from one origin and checked one oracle ray outside its timed loop; all seven single-ray oracle checks reported zero mismatches. ZTorus timed 64 frozen rays repeated 10,000 times, with no application result cache. Distinct geometry, query mix, repetition structure and missing native class/normal controls make this insufficient for the <=1 us or >=80% native targets. Candidate, class/normal native ratios, P95/P99, transport, allocations and full error counts remain null. The stronger frozen-bank replay and matched native query controls are next.

### Exact local restart

From WSL `OpenMC-Dev-D` in this worktree, use the already configured `build/local-cont-20260925`; `ctest --test-dir build/local-cont-20260925 --output-on-failure -j1` repeats the standalone tests. Run `dev/stellarcsg/qualification/local_old_native_sentinel.py --help` for the bounded sentinel coordinator; give it a **new** output path. The committed `old-native-sentinel-03.json` records input/binary/library hashes, loader binding, commands, exits, raw stdout/stderr and repetition order.

## Certification audit recovery preflight

- Downloaded the additive six-file `certification_audit` patch through its saved chat. SHA-256 `2548f0ec4fb8f5ce0e162b830f0b240f335c193384e41de5babdde87f59f5655` matches the review; `git apply --check` passed on the full checkout. The patch only adds audit files and changes no production code.
- The two source headers expected by its runner have Git blob hashes `af3fc2b8a86f6945adeb27b4f46157e172137349` and `2c576398257a7e1f118fa99b99e55d92c4003bc9` in the separate c92 evidence checkout, matching the patch's pinned baseline. Existing WSL `clang++` is version 19.1.7, alongside GCC 14.2.
- Before its bounded local probe build, the refresh showed 3,768,996 KiB Windows RAM free and 88,443,420,672 B C: free. The largest process was `vmmemWSL` at 2.16 GB, followed by ChatGPT 1.13 GB, Chrome 0.94 GB and Codex 0.74 GB. The proposed output `reports/local-cont-20260925/audit-witness-01` does not exist.
- Planned acquisition: 0 bytes. The audit runner caps compiler output at 16 MiB and artifacts at 8 MiB, uses existing GCC/Clang, and is restricted to one CPU and 512 MiB per child. Output target is solely the new audit-witness directory in this continuation worktree; rollback is removal of that exact directory after preserving its receipt. The evidence checkout is a read-only source root.
- Initial audit run stopped before compiling: WSL's cgroup v2 controls live under `/sys/fs/cgroup/init.scope`, and the checked-out headers have CRLF bytes while the expected Git blobs use LF. The failed preflight is preserved in `audit-witness-01/preflight.json`. The runner is being adapted to resolve `/proc/self/cgroup` and retain the same missing-observation block. For exact header bytes, a local `git archive` of the two already-present c08 blobs will be unpacked only into ignored `build/local-cont-20260925/audit-source`; expected source bytes are about 13 KiB, external acquisition 0, new local archive/snapshot under 100 KiB. Rollback is removal of those exact new files after their hashes and receipts are retained. No evidence checkout file is edited.
- Windows `git archive`/ZIP extraction produced CRLF working bytes despite the pinned LF blobs. Replacing only the two ignored snapshot files with `git cat-file blob` bytes yielded exact raw Git blob hashes `af3fc2b8...` and `2c576398...`; no tracked source was changed. The revised runner resolves its own unified cgroup path from `/proc/self/cgroup`, then reads required `memory.max`, `memory.current` and `cpu.max` there. Missing values still block compilation.
- Second run `audit-witness-02` completed with `AUDIT_WITNESSES_REPRODUCED_NOT_SOLVER_PASS`, exit 0: 16 analytic/predicate cases; GCC 14.2 and Clang 19.1.7 probe compile/run exits 0. Both probes report the floating-frame counterexample: derivative x `2.2227587494850775e-162`, rounded speed squared `4.9406564584124654e-324`, tangent squared norm 1.25, evaluated surface z `1.1180339887498949` versus certain bound upper z `1.0000000000000009`. Both also report endpoint-sphere proxy discriminant -2048 while the exact stored ray passes through the center. The source is a two-header focused reproduction; `full_kernel_executed=false`. It does not establish an end-to-end WISTELL-D query failure or certify a repair.

## Frozen-bank replay preflight

- Selectively restored the c92 replay harness and the unchanged 160-row strict bank, without its rejected solver changes. The bank SHA-256 is `7348483e39128259a844d22a7618869a3f111604ea48d01e196a87fe0d396af7`, matching the preserved checkpoint. Its real-coil source file in the separate evidence checkout is read-only and SHA-256 `9bb178c308e7f3ec659e1651a7d57e0ecbff734d3320000df1c2bf79fcf3262b`. This is the prior 1 cm circular diagnostic fixture, not a physical winding-pack acceptance model.
- Before building the one added replay target, the refresh showed 3,284,064 KiB Windows RAM free and 87,996,825,600 B C: free. Largest resident processes were WSL 1.29 GB, ChatGPT 1.20 GB, Chrome 0.94 GB and Codex 0.75 GB. Existing GCC/HDF5/Ninja and the earlier standalone build are reused, one build job only.
- Planned acquisition bytes: 0. Target is `build/local-cont-20260925/stellarcsg_recovery04_bank` and its Ninja object within this worktree, expected under 50 MiB. Rollback is removal of only that target's binary/object after preserving receipts; shared environments and evidence worktrees are untouched.
- The replay target built successfully with one Ninja job. The source is recovered-old `c67`; its binary SHA-256 is `62c8d64b19de2ff20dd390417f4b88037ef526e6e3e6ead65c3d8541af69c28b`. The imported measurement coordinator was changed to reject duplicate lane labels and propagate child failures after preserving every attempt; unlike its original form, it cannot overwrite a lane through dictionary construction or return success after failed children.
- Seven fresh-process strict-bank runs (`strict-old-01`) produced 160 query rows plus one summary each. All seven children exited 1 for the expected-hit a03 failure; their records are complete and reducer errors are empty. Recovered-old distance median of seven complete bank means: **1,088.75 ns/query**, pooled P95/P99 **3,200/6,000 ns**. Classification **1,356.875 ns/query**, P95/P99 **2,000/2,700 ns**; normal **1,545 ns/query**, P95/P99 **2,200/2,900 ns**. Candidates/Newton/subdivision counters are disabled and reported null. These are per-call timers on this one bank, with timer overhead; not ZTorus-matched metrics.
- One separate fresh exact-reference lane, hash `ac526ab00b0444d64d5e04b80bb5abea140381ffcf5673c40641f877795215d9`, completed 160/160 with exit 0 and no block. `compare_frozen_oracle.py` compared the complete bank with matching coefficient/fixture hashes and returned FAIL for **a03, a06, a08, a15**. Old returned 0.502 cm instead of about 0.002 cm for a03; false no-hit for a06's positive ~`8.33e-17` cm contact and a08's 2 cm tangent; and an a15 distance about `3.11e-7` cm early. This reproduces the four preserved wrong-root IDs. The old in-process `distance_reference` gives visibly different numerical values for a06/a08/a15 than this stronger exact lane; it is not promoted as the correctness oracle.
- The a03 expectation alone causes the timed harness's nonzero exit; with `--skip-reference`, a06/a08/a15 appear as ordinary PASS rows. The separate exact comparison is therefore mandatory for root correctness. No corrected candidate has been built or timed; no performance promotion is warranted.

## Final-iterate residual experiment preflight

- Preserved the recovered-old statically linked replay binary at ignored `build/local-cont-20260925/controls/old-replay`; its SHA-256 exactly matches `62c8d64b...`. This allows same-session old/candidate replays after an incremental relink.
- Before the incremental build, Windows RAM free was 4,190,988 KiB and C: free 87,631,781,888 B. Largest resident processes were WSL 1.27 GB, ChatGPT 1.17 GB, Chrome 0.95 GB and Codex 0.76 GB. GCC/HDF5/Ninja and the local build are already present.
- Planned external acquisition bytes: 0. One Ninja job will update only the continuation worktree's standalone reference library, replay binary and existing test executables, expected under 50 MiB new output. Rollback is to rebuild from the preserved old source commit or use the hash-verified saved old binary; the old binary and all evidence checkouts are not modified. The candidate is experimental and the OpenMC option remains default OFF.
- Cherry-picked the 21-line final-iterate residual recheck as `49e16439069a2fa713fff611d5baac2bfc741f15`. One-job incremental build passed; both standalone CTest cases passed. It rejects nonfinite or over-tolerance final Newton residuals, but does not establish root existence or nearest-root completeness.
- First paired seven-repeat strict run (`final-residual-01`) had complete raw rows and zero reducer errors. Old/candidate distance medians were 1,143.125/1,221.86875 ns/query (candidate/old 1.0689). The separate exact-lane comparison still failed on a03, a06, a08, a15; no strict correctness gain. The Windows reducer initially hit a path-string portability error on receipt CSV keys; that bug was fixed without changing reduction math, and the complete receipt reduced successfully.
- A new interleaved seven-repeat session (`final-residual-native-01`) measured native OpenMC ZTorus before and after every old/candidate pair, checking `ldd` binding to the hash-bound native `libopenmc.so`. All 14 native samples were valid; median 176.6399234375 ns/query, range 170.5621890625–192.4992203125. On the same 160-query coil bank, old median was 1,161.875 ns/query and candidate median 1,385 ns/query, candidate/old **1.1920**. Old/native and candidate/native arithmetic sentinel cost ratios were **6.5776** and **7.8408**. They are labeled `UNLIKE_GEOMETRY_AND_BANK_SENTINEL_ONLY` because ZTorus repeats a 64-ray torus bank; they are not matched fidelity or transport comparisons.
- The exact four wrong roots remain; native classification/normal and transport controls, candidate/Newton counts, fallback time fractions, allocations and memory remain null. The candidate median exceeds the 1 us aspiration on this bank. The residual correction stays experimental; no fast corrected kernel is qualified or promoted.

## Transport-contract patch preflight

- Retrieved and hash-verified both remaining cloud patches into the private, nonpublic `chatgpt-pro-handoff/review-20260925/recovered-patches/` directory, with a manifest. `transport-contracts.patch` is `caa5184fce8905757bc75723d234bb0310ba9c008f1d236ee3f8cde8479570e7`; `plasma-source.patch` is `42fac22345ea0b7f32881488dc75f3283773ac0c9c4dc1f7b4fce28c945427b6`. Both pass `git apply --check` against this full checkout. The plasma patch remains unapplied while UQ's interchange schema is provisional and its heldout rates fail. The transport patch was applied for focused local validation; it changes only adapter guards, Python albedo import and opt-in contract tests, not the numerical solver.
- UQ task reported a provisional class-B field list: Cartesian cm vertices, physical VMEC `s` labels, tetra connectivity with positive volumes, separate normalized element probabilities, source case phase/evidence/profile/energy laws and mesh/data hashes, `support_s_max`, and external `physical_rate_n_s` (null until established). It cautions that the current s=1 source crosses the first wall and a generic global displacement is inadmissible for the 90-degree sector. This is not an accepted StellarCSG interface contract; source integration remains default OFF.
- Before building the focused C++ adapter isolation test, Windows RAM free was 3,828,884 KiB, C: free 87,573,082,112 B, D: free 505,893,134,336 B. Largest processes were WSL 1.23 GB, ChatGPT 1.20 GB, Chrome 0.97 GB and Codex 0.76 GB. WSL affinity includes CPU 0–11. Existing GCC/HDF5 1.14.5, NumPy 2.5.1, lxml 6.1.1, h5py 3.16.0 and pytest 9.1.1 suffice; no installation/download is planned.
- New build/output target: `reports/local-cont-20260925/transport-contracts-01`, initially absent. Planned external acquisition bytes 0 and local generated bytes under 50 MiB; the script caps each compiler child at 1.5 GiB virtual memory and one CPU. Rollback is removal of that exact new output directory after preserving the results; no shared build or UQ checkout is modified. The expected controlled-test result is seven contract failures, so a nonzero exit is evidence, not a pass.
- On first invocation, Windows CRLF checkout bytes caused Bash to fail before any build. Converting only the opt-in runner to LF fixed that local portability issue. The controlled C++ adapter suite then reproduced **40/47 PASS, 7 FAIL**, exit 1, with the exact block codes listed in `transport-contracts-01/results.jsonl`. Python method isolation reproduced **18/20 PASS, 2 FAIL** for missing collection XML/HDF5 import. Exact analytic ownership tests passed 16/16. Unlike the cloud selected-file environment, this full checkout's native Python API tests executed: 12 passed and two remained explicitly expected-failure equality cases; no native transport was run.
- A narrow collection edit is under test: reject duplicate member IDs, choose the lowest stable ID at an exactly tied hit or normal ownership, and throw on nonfinite member classification/hit results before selection can hide them. This does not solve overlap union boundaries or terminal unresolved status. Periodic images of a physical coil will require explicit distinct member IDs while retaining physical-coil identity separately; this provisional guard is not a complete period identity schema.
- Before recompiling the controlled suite, Windows RAM free was 5,484,824 KiB and C: free 87,127,060,480 B. Top processes were ChatGPT 1.10 GB, Chrome 0.97 GB and Codex 0.77 GB. Existing GCC/HDF5 and one CPU suffice; planned external acquisition 0 B, local output under 50 MiB at the new `transport-contracts-02` directory. Rollback is the exact new directory after preserving its results; the first negative receipt remains untouched.
- The second actual-adapter controlled run completed **43/47 PASS, 4 FAIL**, exit 1. Duplicate member IDs, deterministic equal-hit ownership, and nonfinite member hits now pass their focused cases. The four failures are the true-overlap union boundary and three returned-unresolved terminal-status cases; the results remain failed in `transport-contracts-02/results.jsonl`. A minimum ID tie is only stable bookkeeping, not a unique physical normal proof. The member ID restriction remains provisional for repeated periodic images.
- Before incremental standalone rebuild, Windows RAM free was 5,677,260 KiB, C: free 87,548,104,704 B and D: free 505,860,579,328 B. Largest processes were ChatGPT 1.21 GB, Chrome 0.97 GB, Codex 0.77 GB; the existing Ninja build and WSL toolchain are present. Planned external acquisition is 0 B. One build job will update only `build/local-cont-20260925` (under 50 MiB expected incremental output); rollback uses the retained old binary and rebuild from the branch's prior commit. This rebuild validates the collection change in the real standalone library rather than only the controlled double suite.
- The incremental real standalone build succeeded with one job; both CTest cases passed. This checks compilation and existing standalone cases, not native OpenMC transport or overlap geometry. The Python method suite still reports 18/20, with only opaque collection XML/HDF5 representation missing. A Python collection object alone would not expose member/material ownership to OpenMC tallies, so no importer change is claimed as a transport fix.

## Current checkpoint and next admission gates

- The opt-in adapter contracts and bounded collection guards are committed on the isolated branch. Their actual outcomes remain 43/47 C++ controlled, 18/20 Python methods, 16/16 analytic ownership, 12 native Python API passes with two expected failures, and 2/2 standalone CTest; no native particle transport or full OpenMC C++ build has run for this patch.
- Source inspection confirms that the compiled swept solver can reach an ordinary no-hit after a finite eight-segment sign scan, with no terminal unresolved outcome. Historical `unresolved_intervals` counts cannot by themselves distinguish a resolved fallback from a terminal failure. The next solver change must carry explicit hit/no-hit/unresolved disposition and earlier-prefix completion through the member, set and OpenMC adapter; treating the existing counter as that status would reject valid resolved cases and still miss some unresolved cases.
- The reproduced frame escape and negative endpoint-proxy discriminant remain focused two-header witnesses, not production-class qualification. Correctness requires numerical enclosures for the actual evaluated frame and conservative retained-domain handling for proxy failures before a root speedup can be promoted. The strict frozen bank still has four wrong roots in recovered-old and the final-residual experiment, so the 1 µs/2 µs and native-throughput goals remain open.
- The current 1 cm circular-coil fixture is diagnostic. Winding-pack/blanket fidelity, image ownership, one-period closure, matched generic import, native C++/C API transport, same-geometry ZTorus performance, and the UQ-coordinated bounded plasma source remain unqualified. The plasma-source patch stays unapplied while the UQ interface and wall-bounded rate evidence are provisional.

## Terminal unresolved contract continuation

- The preceding turn made progress (commits `7e5c48339`, `e46fcf7d2`, `63f491953`); the current worktree began clean. This continuation adds a typed `hit`/`no_hit`/`unresolved` disposition to `DistanceResult` without using historical unresolved counters as terminal state. The swept general solver now retains zero-proxy candidate spans, reports unsolved earlier spans and traversal overflow as terminal unresolved, and the set/adapter propagate that outcome. This is a focused failure-preservation repair, not a nearest-root certificate: a successful seed does not prove that its span's earlier prefix is root-free, and the floating bounds remain unaudited.
- Before rebuilding, Windows free RAM was 5,606,564 KiB of 33,457,440 KiB; C: free space 87,188,348,928 B and D: free space 505,832,251,392 B. Largest resident processes were ChatGPT 1.26 GB, Chrome 0.98 GB and Codex 0.77 GB. The existing one-job WSL GCC/HDF5/Ninja build and Python environment remain sufficient; no download, installation or environment modification is planned. New controlled output target `transport-contracts-03` was absent. Planned external acquisition: 0 B; expected local controlled output under 50 MiB and incremental standalone build output under 50 MiB. Rollback is removal of that exact new controlled output after retaining its receipt plus rebuilding from commit `63f491953`; the previous build/control receipts are preserved.
- Controlled actual-adapter test result: **47/48 PASS, 1 FAIL**, exit 1. All terminal-unresolved propagation checks pass; the new resolved-historical-count check passes; true-overlap exterior union distance remains the sole failed case. The one-job real standalone build passed 2/2 CTest. The frozen replay harness now treats a terminal unresolved result as `BLOCKED` and writes `candidate_disposition`; it was rebuilt successfully but a fresh bank replay is still required to quantify its disposition.
- Fresh hash-bound 160-query strict replay at commit `767838587` has 21 `hit`, 88 `no_hit`, and **51 terminal `unresolved`** dispositions; all 51 are `BLOCKED` in the harness, including the four previously wrong-root IDs a03, a06, a08 and a15. A comparison to the same-coefficient/source exact control reports exactly those 51 as BLOCKED, with no further mismatch among the 109 nonblocked rows. This is a bounded-corpus observation, not proof that the remaining 109 are complete in general. The receipt and comparison are `strict-terminal-01`; exit 2 from the replay and exit 1 from the comparison are retained, not relabeled PASS. No performance qualification is attempted on 51/160 blocked queries.

### Full native OpenMC integration build preflight

- The isolated full OpenMC checkout's `vendor/fmt` and `vendor/pugixml` directories are empty; corresponding already-local submodule sources in the actual repository checkout match the required Git commits `0c9fce2f...` and `ee86beb3...`. System fmt/pugixml development packages and CMake configurations were not found; CMake would otherwise fall back to the empty submodule directories. Catch2 is not required with `OPENMC_BUILD_TESTS=OFF`. No source will be downloaded or installed.
- Refreshed Windows RAM free: 4,670,908 KiB of 33,457,440 KiB. C: free: 86,500,892,672 B; D: free: 505,785,114,624 B. Largest processes: WSL 1.27 GB, ChatGPT 1.12 GB, Chrome 0.97 GB, Codex 0.74 GB. Existing WSL GCC 14.2, Ninja, CMake 3.31.6, HDF5 1.14.5, `/opt/openmc-venv`, package caches and local checkouts were recorded in the initial audit. The new `build/native-local-20260925` target is absent.
- Planned external software acquisition bytes: **0**. Two junctions inside this isolated worktree will reference the exact already-local fmt/pugixml sources; they copy 0 bytes and leave the source checkout untouched. The native experimental build is capped to one compiler job, with local output budget under 2 GiB on C:. Rollback is removal of only the exact new native build directory and those two junctions after retaining receipts; no shared build, dependency environment, evidence checkout, remote system or source branch is modified. A configure failure will be reported rather than triggering an unreviewed package acquisition.
- The junction attempt failed without changing either target: Windows reported the empty OneDrive submodule placeholders are read-only reparse points and denied removal/creation. This fallback is still local-only: the already-built `fmt` and `pugixml` CMake package configurations in the main OpenMC checkout point to present static libraries (`libfmt.a` 244,906 B; `libpugixml.a` 342,992 B) and the exact locally initialized submodule headers. The new build will use their explicit `fmt_DIR`/`pugixml_DIR`; no source directory, shared library, or environment is modified. This means the native integration build depends on those hash-matched local development artifacts, which will be named in its receipt.
- First configure failed before compilation: CMake's default `GIT_SUBMODULE=ON` tried `git submodule update --init --recursive`, which could not interpret this Windows-created worktree `.git` path in WSL. No network acquisition occurred. CMake also had an unconditional vendored pugixml existence check before `find_package`; I moved the missing-source checks into the respective package fallback branches. The retry will set `GIT_SUBMODULE=OFF` and retain the explicit local package directories. Git-based version detection may still report 0.0.0 in this WSL/Windows worktree; that is metadata only and will be recorded rather than treated as a qualified release version.
- Second configure passed with `GIT_SUBMODULE=OFF`, experimental StellarCSG ON, OpenMP/MPI/DAGMC/tests OFF, local HDF5 1.14.5, pugixml 1.15 and fmt 11.0.2 from the previously built local CMake packages. Git-derived OpenMC version did report 0.0.0 because WSL cannot interpret this Windows worktree `.git` pointer; no fetch was attempted. Before the one-job native compile, Windows RAM free was 3,753,984 KiB, C: free 88,041,873,408 B and D: free 505,732,161,536 B; top processes were WSL 1.65 GB, ChatGPT 1.14 GB, Chrome 0.98/0.97 GB and Codex 0.69 GB. Planned new acquisition remains 0 B. Build output remains confined to `build/native-local-20260925` under the 2 GiB local budget.

## Rotational frame fidelity inventory

- The original qualified 48-member WISTELL-D HDF5 has twelve four-member 90-degree centerline rotation cycles. Sixteen member centerlines straddle a 90-degree seam at their control points, so assigning whole coil surfaces to a sector solely by centroid is insufficient. Maximum control-point centerline misalignment after the best cyclic match is 0.0002030704 cm, a diagnostic of the compiled input, not a continuous spline or accepted symmetry tolerance.
- In the original payload, sixteen rotated pairings differ by more than 0.01 in their supplied normal controls; maximum normal difference is 1.4463 and maximum elliptical cross-section tensor difference is 51.5400 cm² (control-point metric). Source inspection found `_rotation_minimizing_frame` used a fixed global x fallback for near-vertical tangents. That fallback is not covariant under a 90-degree stellarator rotation, so separately compiled elliptical copies can have different physical section orientations despite near-matching centerlines.
- The compiler now uses the local cylindrical radial direction for that fallback when the start point is safely away from the axis; its near-axis generic fallback remains explicit. A dedicated near-vertical 90-degree frame test passed. Recompiled the pinned four-period/48-coil source into a **separate** candidate HDF5 (`wistell-period-frame-candidate.h5`, SHA-256 `9571b71cff1f2f1e4bbeb079d779bfcc9b614da87e16cf2cfd49dae931918018`) with the same 10/8 cm section radii, 256 controls and distinct frame-seed metadata. The original qualified HDF5 was untouched.
- In that candidate, maximum rotated normal-control difference fell to 1.319e-7 and maximum ellipse-tensor difference to 1.859e-5 cm²; the 0.0002030704 cm centerline mismatch and sixteen seam-crossing members remain. These are point-control diagnostics only. The MAKEGRID input defines filaments, not a validated winding-pack outline or one-period material assembly, so no physical fidelity or transport pass is claimed. All 15 local StellarCSG Python tests pass, including the new frame symmetry test.
- The separate UQ task's latest fixed-wall clearance receipts reject its nominal source (strict first-wall crossing), 32-poloidal refinement (zero-distance facet pair) and wall-aligned grid (218 inverted tetrahedra). No plasma source from those meshes is accepted into this OpenMC model; the bounded source interface remains on hold pending a positive-volume, wall-clearing mesh and an accepted source rate.

### Native adapter smoke preflight

- The full experimental `libopenmc.so` compiled and linked all 135 build steps with one job; the actual periodic/swept adapters and solver sources were compiled, using already-local fmt/pugixml static libraries. This is a native C++ integration build, not a tracker/transport run.
- Before compiling a small native surface-registration smoke executable, Windows RAM free was 4,205,916 KiB, C: free 87,003,750,400 B and D: free 505,702,023,168 B; largest processes were WSL 1.55 GB, Chrome 1.42 GB, ChatGPT 1.33 GB and Codex 0.71 GB. The existing GCC/HDF5/native library and already-local package headers suffice. Planned acquisition: 0 B. New target `build/native-local-20260925/native_adapter_smoke` was absent, expected output under 20 MiB; rollback is removal of that exact executable after preserving the receipt. It will use the analytic HDF5 fixture, not the WISTELL physical assembly.
- The manually linked native smoke initially lacked the fmt public header include; adding the path to the already-local exact-version fmt headers produced the executable. Its first run failed at the collection query. A second run distinguished stages: the single analytic swept surface registered through actual `read_surfaces` and returned a finite 25 cm distance; the three-member collection threw `Swept-spline member has an unresolved nearest-boundary query`. The hash-bound `native-adapter-01` receipt verifies `ldd` resolves the newly built `libopenmc.so`, source/binary/input hashes, one-CPU affinity, child exit 1, one single-surface PASS, collection BLOCKED and `native_transport_run=false`. This is a real native integration failure for that collection ray, not evidence of particle tracking or one-period performance.

## First-quadrant span admission diagnostic

- `qualification/sector_span_inventory.py` takes the hash-bound rotational inventory and the same HDF5 input. For every periodic cubic span, its four control points bound the continuous centerline through the nonnegative B-spline basis. The largest positive major/minor radius control bounds the surface displacement because the C++ kernel normalizes the transverse frame. Outward-padded span boxes are admitted if they can touch the closed first quadrant (`x >= 0`, `y >= 0`). This is a broad geometric candidate filter, not a directed floating-point proof of the C++ evaluation or an exact surface/sector intersection.
- The original full-device HDF5 and the revised-frame candidate produce the same admission: **18 members and 3,156 of 12,288 spans**. Six of the twelve 90-degree rotational families admit two images; six admit one. The candidate set is `coil_1000`, `1001`, `1002`, `1007`, `1008`, `1009`, `1010`, `1015`, `1016`, `1023`, `1024`, `1031`, `1032`, `1039`, `1040`, `1045`, `1046`, `1047`. The 30 disjoint member boxes may be excluded from a first-quadrant candidate list at this diagnostic level. Nine admitted spans on each of `coil_1009` and `coil_1010` have no box wholly inside the quadrant, showing why an all-or-nothing centroid selection is unsafe.
- Reproducible hash-bound receipts are `local-cont-20260925/sector-span-baseline-01.json` and `sector-span-candidate-01.json`. The 18 members remain whole periodic surfaces; these receipts do not trim them to the sector, set periodic plane identity/normal ownership, prove continuous rotational equivalence, or validate physical winding-pack or transport behavior. An actual one-period OpenMC assembly must resolve those contracts before use.

## Overlapping collection boundary continuation

- The preceding goal turn made progress with commit `1f9c464cb` and left a clean worktree. Inspection confirmed the collection distance routine returned the first constituent boundary even when another member still contained that point. For two overlapping analytic members this returned an interior crossing at 2 cm instead of the union exterior exit at 3 cm. Changing only the selected member ID or normal would not repair the distance.
- The collection now retains its BVH first-hit path when no other member contains the hit. If another member masks that hit, it constructs ordered member events, advances the masked member from its own boundary with coincident-origin suppression, and returns the earliest unmasked event. Nonfinite/nonadvancing events, terminal unresolved member queries and an exhausted event budget throw rather than become no-hit or an interior hit. This is a runtime union-boundary repair; the constituent solver's nearest-root and floating-bound gaps remain.
- A fresh local build audit is `local-cont-20260925/build-audit-20260925-04.txt`: 3,217,436 KiB RAM free of 33,457,440 KiB, C: 86,894,907,392 B free, D: 505,667,219,456 B free, WSL/ChatGPT/Chrome/Codex largest residents, existing GCC 14.2/CMake 3.31.6/Ninja 1.12.1/HDF5 1.14.5 and Python environment/cache/checkouts. No software acquisition or environment change; one-job incremental builds in the existing worktree only.
- The focused actual-adapter/analytic-member controlled suite returned **49/49 PASS**, compile exit 0 and child exit 0 in `transport-contracts-04`. It covers the previously failing 3 cm exterior union exit and a masked entry where the same member's later exit at 5 cm is the first exterior boundary. The real standalone compiled-surface test now also constructs overlapping swept circular tori and verifies the later 0.55 cm union exit and owning member. One-job standalone rebuild and CTest **2/2 PASS**. These checks do not establish generic overlap/material ownership policy, native OpenMC tracking, WISTELL assembly transport or root completeness.
- A refreshed native-build audit addendum recorded 4,546,632 KiB RAM free, C: 86,865,649,664 B and D: 505,621,082,112 B free, existing native build/configuration and 3,821,328-byte library, and no software acquisition. The one-job native incremental build passed all four steps, including recompiling the collection and relinking `libopenmc.so` and `openmc`. An initial smoke invocation with relative paths failed its loader-binding preflight because the runner compared a relative `ldd` path against an absolute one; no child smoke was run. The runner now resolves all input paths before setting `LD_LIBRARY_PATH` and hashing. The final `native-adapter-03` receipt binds the rebuilt library SHA-256 `31578a499d905e0c1c12fd71270e39a7cf905cc3d5d12cb73c412cdf5707f65a`; native single-surface registration still returns 25 cm, while the same three-member collection still stops explicitly with an unresolved nearest-boundary member query (child exit 1). No native particle transport ran.

## Native unresolved-member localization

- The preceding goal turn made progress with commit `8a936b7b5` and a clean worktree. The native collection smoke was extended to query analytic members 2 and 3 individually through real `read_surfaces`, then directly inspect each compiled solver's lead and terminal disposition. Member probes are diagnostic only and keep the collection's failure exit. Before rebuilding the 29,248-byte helper, `build-audit-20260925-05.txt` recorded 3,097,732 KiB free Windows RAM, C: 86,401,675,264 B and D: 505,601,814,528 B free, top ChatGPT/Chrome/Julia/Codex processes, existing WSL GCC/CMake/Ninja/HDF5, Python environment/cache and source checkouts, planned external acquisition 0 B, a one-job helper target under 20 MiB and exact rollback.
- The first manual compile lacked the existing generated `build/native-local-20260925/include/openmc/version.h` path and failed before linking; adding that include path succeeded. `native-adapter-04` showed that **both** elliptical/nonplanar analytic members, not just the collection wrapper, throw unresolved through the native adapter. `native-adapter-05` then exposed raw candidate leads: member 2 has a 16 cm lead with four unresolved intervals; member 3 has a 17 cm lead with five. Both results explicitly have `found=true` and `terminal_unresolved=true`.
- A bounded follow-up in `native-adapter-06` ran the slower global reference search on exactly those two rays. It also reports 16 and 17 cm; this is agreement on a diagnostic input, not a proof that earlier roots are absent. The native adapter continues to reject both individual queries and the collection, child exit 1; the 25 cm circular member still registers and responds. The receipt binds the rebuilt native `libopenmc.so` SHA-256 `31578a49...` and probe helper SHA-256 `675286d5...`, records one-CPU affinity and states `native_transport_run=false`.
- A separate control-hull box inspection found eight spans per shaped member whose broad radius-inflated boxes can reach the ray before the candidate lead. Member 2's earliest such box entry is about 5.936 cm; member 3's boxes even extend behind the 550 cm ray origin. These boxes are intentionally broad and are not the solver's exact unresolved-span list, but they explain why a matched reference lead cannot be promoted without an earlier-domain exclusion proof. The next root-completeness repair must certify those prefixes or keep the terminal block; merely returning the 16/17 cm leads would weaken correctness.
- The separate UQ task remains at its latest verified rejected-source state: nominal source/first-wall strict crossing, refined source facet contact, and 218 inverted tetrahedra on the wall-aligned grid. It has no accepted motion mesh or rate for the StellarCSG bounded plasma-source interface.

## Swept-prefix interval feasibility experiment

- The preceding goal turn made progress with commit `d92ff417b` and a clean worktree. The two native shaped-member leads are blocked by earlier-domain uncertainty, so `qualification/probe_swept_prefix_intervals.py` tests a candidate mathematical exclusion method **off the production path**. It reconstructs periodic cubic span powers from the hash-bound analytic HDF5, encloses center, tangent, normalized frame and radii over subdivisions, and projects the elliptical section onto the two coordinates perpendicular to the ray. When the projected 2×2 system is nonsingular, interval solutions for `cos(alpha)` and `sin(alpha)` can be ruled out by the unit-circle constraint or by their along-ray extent. Near singular knots, a single projected equation plus the two complementary unit-circle branches can bound the ray coordinate. The Python intervals expand primitive results with `nextafter`, but this experiment does not enclose the C++ coefficient-construction and evaluation path and does not modify any terminal disposition.
- Before refreshing the native probe helper, `build-audit-20260925-06.txt` recorded 3,229,104 KiB RAM free of 33,457,440 KiB, C: 85,888,602,112 B and D: 505,482,510,336 B free, top ChatGPT/Julia/Chrome/Codex processes, existing GCC/CMake/Ninja/HDF5, Python environment/cache and local source checkouts. External acquisition was 0 B; one helper rebuild in the existing native build directory was under 20 MiB. Its compile passed. The full-precision `native-adapter-07` receipt binds the same `libopenmc.so` SHA-256 `31578a49...` and gives member 2 fast/reference leads `16`/`16.000000000672649` cm and member 3 `17.000000000000227`/`17.000000000038547` cm. Both fast results remain terminal unresolved (four/five intervals), both native member adapters throw, and the collection child exits 1.
- The final hash-bound prototype receipts are `prefix-interval-probe-13.json` (cutoff at lead minus **1e-11 cm**) and `prefix-interval-probe-14.json` (zero gap), each at depth 36 and 10,000 nodes per span. Of 16 broad control-hull candidate spans across members 2 and 3, the interval model excludes **16/16** before the gapped cutoff in 188 total nodes, but only **13/16** with zero gap. That distinction correctly retains the knot-local root region at the exact lead. Negative controls move the cutoff 4 cm later, enclosing a known root; the model leaves one member-2 span and two member-3 spans undecided. The script asserts those negative controls. Earlier superseded scratch receipts were removed after preserving these final runs.
- These are feasibility and regression observations, **not** a root-completeness certificate. Production admission still needs interval bounds tied to the actual C++ `span.power` and floating evaluation, a proof of the finite root tile and its uniqueness/ordering within the 1e-11 cm gap, controlled singular-frame behavior, and a bounded performance study on the strict 160-ray bank plus real WISTELL coil data. The native adapter remains fail-closed and no one-period transport or speed claim is promoted.

## Compiled-power interval feasibility continuation

- The preceding goal turn made progress with commit `dc7204ce5` and a clean worktree. `experiments/swept_prefix_interval.cpp` now evaluates interval enclosures over the **actual stored C++ `SweptSpan.power` arrays** exposed by each compiled member, rather than reconstructing powers from HDF5 in Python. It projects each elliptical section with algebraic ray equations, avoiding a numerically approximate perpendicular basis. The experiment is a separate executable linked to the local native `libopenmc.so`; production distance and adapter code are unchanged.
- The current-computer acquisition audit is `local-cont-20260925/build-audit-20260925-07.txt`: initial free RAM 4,870,456 KiB of 33,457,440 KiB; C: 86,633,058,304 B and D: 505,464,274,944 B free; top ChatGPT/Chrome/Codex processes; existing WSL GCC 14.2, CMake 3.31.6, Ninja 1.12.1, HDF5 1.14.5, Python environment/cache, native library and source checkouts. Five bounded one-job helper builds were refreshed against live RAM/disk/process observations; no software was acquired or environment changed. The experiment was compiled with `-O2 -fno-fast-math -ffp-contract=off`, under 20 MiB output on C:.
- The final hash-bound `cpp-prefix-06` receipt records native library SHA-256 `31578a49...`, experimental binary `63cc9a11...`, analytic HDF5 `39f77da5...`, one-CPU affinity, IEEE-nearest/subnormal preflight and child exit 0. It checks **every span**, without the C++ BVH/bounding-box prefilter: 256/256 member-2 spans and 384/384 member-3 spans are excluded from `0 <= t < lead - 1e-11 cm` by the experimental interval model, in 326 and 492 tile visits. At the exact lead, one and two spans remain undecided; moving the cutoff 4 cm later also leaves one and two known-root spans undecided. An underflowed tangent control returns `undecided_frame`, not an exclusion. The receipt verifies all 640 per-span rows and their summaries. Earlier `cpp-prefix-01` through `05` receipts are retained as the method was tightened and expanded.
- **No production root has been admitted.** This is still an experimental arithmetic model: its `1e-12` unit-circle allowance for C++ `sin`/`cos` is not an audited library error bound, the compiled floating path and rounding environment need independent review, and the remaining narrow root tiles need existence, uniqueness and ordering proofs. The 1e-11 cm exclusion gap is not itself a root certificate. The native shaped-member and collection queries remain terminal unresolved; strict frozen-bank, WISTELL one-period, transport and matched performance gates remain open.

## Rectangle-only prefix comparison

- The compiled-power interval experiment now runs a second analysis for every span with both circular-identity exclusions disabled. That mode uses only independent cosine and sine ranges `[-1, 1]`, projection equations and along-ray prefix bounds. It is a dependency diagnostic, not a formal enclosure of the evaluated C++ surface. The production solver and native adapter are unchanged.
- The local build audit is `local-cont-20260925/build-audit-20260925-08.txt`: 3,909,420 KiB of 33,457,440 KiB Windows RAM free, C: 85,992,919,040 B and D: 505,352,536,064 B free, significant processes and the existing GCC 14.2/HDF5/native toolchain, Python environment, caches and local checkouts. Existing capabilities sufficed; software acquisition was 0 B. A separate 41,792-byte experimental executable was compiled with one GCC job and `-O2 -fno-fast-math -ffp-contract=off` into the existing native build directory. No dependency environment was changed.
- The final no-overwrite `cpp-prefix-rectangle-02/receipt.json` binds the experimental executable, native `libopenmc.so`, analytic HDF5 and source hashes. At the same lead-minus-1e-11 cm cutoff, rectangle-only analysis excludes **256/256** member-2 spans and **382/384** member-3 spans. The undecided spans are periodic seam spans **0 and 383** of member 3; they each required 73 tile visits under the depth-36 limit. Thus the existing 640/640 circle-assisted exclusion depends on the unaudited unit-circle allowance for exactly those two spans. Every prior per-span field (node count, prefix result and both root controls) matched `cpp-prefix-06` exactly, 640/640; the new receipt also checks unique span keys, summary totals, negative-control totals and monotonicity of rectangle versus circle-assisted exclusion.
- This narrows the arithmetic audit target but admits no root. The rectangle mode still assumes the two evaluated trigonometric values lie in `[-1, 1]`, and neither mode has a complete bound for the production floating path or a finite root-tile existence/uniqueness proof. Both shaped-member queries and the native collection remain terminal unresolved; no performance or transport qualification follows from this experiment.

## Circle-slack and cutoff-gap sensitivity

- [GNU libc's known-error table](https://ftp.gnu.org/old-gnu/Manuals/glibc/html_node/Errors-in-Math-Functions.html) describes the largest errors exposed by existing tests and explicitly notes the large unsearched input space. Its reported sin/cos ulps are therefore observational evidence, not a uniform bound that can discharge this probe's `unit_circle_slack` assumption.
- The hash-bound `cpp-prefix-slack-01/receipt.json` tested slacks `1e-10`, `1e-8` and `1e-6` at the prior 1e-11 cm lead gap. Member 2 remained 256/256 excluded, but member 3 fell from 384/384 at slack `1e-12` to **382/384** for each larger slack. Only periodic seam spans 0 and 383 remained undecided. This makes the earlier all-span result sensitive to slack in the same narrow neighborhood as the candidate lead.
- A second one-job rebuild swept cutoff gaps `[1e-11, 1e-8, 1e-5]` cm against slacks `[1e-12, 1e-8, 1e-6]` on every compiled span. `cpp-prefix-matrix-02/receipt.json` reports member 3 exclusion counts `[[384,382,382],[384,382,382],[384,384,384]]`; member 2 remains 256/256 in all nine cells. The wider 1e-5 cm gap therefore leaves a candidate finite root neighborhood while tolerating the experimental 1e-6 circle allowance on this fixture. That allowance is still not an audited bound for the production libm path, and root existence/uniqueness in the finite neighborhood is unproved. The native adapter remains terminal unresolved.
- The current-computer build audit is `local-cont-20260925/build-audit-20260925-09.txt`. Before the two separate experimental builds, Windows free RAM was 3,596,660 and 4,914,508 KiB of 33,457,440 KiB; C: free was 85,809,049,600 and 85,519,314,944 B, D: free was 505,323,692,032 and 505,296,384,000 B. It records significant processes, existing toolchain/environments/caches/checkouts, two new one-job targets under 20 MiB each, zero software acquisition, and exact rollback. The final matrix receipt also binds the verifier script hash and checks all 640 unique spans, summary counts, gap/slack monotonicity and known-root controls. Its prior per-span fields match `cpp-prefix-rectangle-02` exactly, 640/640. No production solver, native library or transport run changed.

## Rounded local-parameter domain and accepted-residual stress test

- The experimental interval walk previously began at `u=[0,1]`, whereas `frame_in_span` computes `u=(angle-angle_min)*fl(1/(angle_max-angle_min))`. The walk now starts from outward interval evaluation of that exact local-coordinate expression over each stored span endpoint range. This also covers small rounded-endpoint excursions for future inputs. On the two current analytic members, the evaluated upper endpoint did not exceed `nextafter(1,+inf)` in any of 640 spans, and all prior per-span node/exclusion/control fields stayed identical after the wider domain, 640/640. This fixes a general domain-coverage assumption without claiming a witnessed previous false exclusion on this fixture.
- The compiled general solver accepts a finite projected residual up to `max(absolute_f_tolerance*characteristic_length, 64*DBL_EPSILON*characteristic_length)`. The separate experiment now pads each transverse equation for this fixed axial-ray fixture by `2*projected_tolerance + 256*DBL_EPSILON*characteristic_length`, while using unit-circle allowance `1e-6` and cutoff `lead-1e-5 cm`. The final hash-bound `cpp-prefix-padded-03/receipt.json` reports slacks 5.983976786e-7 cm (member 2) and 6.501468937e-7 cm (member 3), with **256/256 and 384/384** spans excluded before the cutoff. At exact lead and at lead+4 cm, the padded method leaves the two seam spans of each member undecided, so known-root controls remain active. The receipt checks all 640 unique span rows and totals, and binds the verifier script, executable, native library, source and HDF5 hashes.
- The machine audit is `local-cont-20260925/build-audit-20260925-10.txt`: three bounded one-job experimental builds with refreshed RAM/C:/D:/process observations, existing GCC/HDF5/Python/caches/checkouts, planned acquisition 0 B, under-20-MiB output targets and exact rollback. No production library or solver was rebuilt or changed.
- This remains **experimental**, because the padding is a stress allowance rather than a proved enclosure of all production floating operations; the C++ `sin`/`cos` relation, singular-frame cases, and existence/uniqueness/ordering of root tiles inside the 1e-5 cm gap still need independent proof. It does not clear terminal unresolved, certify the strict ray bank, or qualify native one-period transport/performance.

## Python collection serialization and payload identity

- The native C++ swept adapter already accepts `dataset_prefix`, `dataset_start` and `dataset_count` as a single union surface, but Python `SweptSplineSurface` could previously construct/import only one `dataset` plus `content_id`. The Python class now supports the same mutually exclusive collection selector, validates a positive count and a nonnegative signed-32-bit index range, emits canonical collection XML without a single-member content ID, and imports the actual C++ HDF5 summary fields. It rejects mixed single/collection selectors rather than silently dropping them. A collection remains one surface; separate surfaces are needed for per-coil cell/material or tally ownership.
- Python collection bounding boxes now union the selected HDF5 member control-hull bounds and add the same characteristic-length rounding inflation used by the C++ swept span builder. Both swept and periodic payload surfaces now define `is_equal` by exact payload-reference fields rather than passing empty polynomial coefficients into the generic normalizer. The comparison is conservative reference identity; for a collection it does not cryptographically verify each mutable member payload, so it is not a full physical-image identity schema.
- No software acquisition, dependency environment change or C++ build occurred in this continuation. The final controlled method receipt `python-method-collection-04/receipt.json` binds `openmc/surface.py`, the actual C++ adapter HDF5 fixture and the runner hashes: **27/27 PASS**, up from the prior 18/20 with two collection-import failures. The final native-package/unit receipt `python-surface-final-02/receipt.json` binds the Python class and test sources and records **45/45 PASS** for collection XML/HDF5 and full geometry XML round trips, selector validation, conservative bounds, single/collection equality and existing surface regressions. Earlier intermediate receipts are preserved. No native C++ particle transport ran; shaped-member root queries and the one-period/fidelity/performance gates remain open.

## Provisional one-period member identity and selector binding

- The new `qualification/build_period_candidate_manifest.py` binds the actual original WISTELL-D coefficient HDF5, the 90-degree rotation inventory and the first-quadrant span inventory before assigning any identity. It checks twelve disjoint four-image cycles, 48 HDF5 `coil_id`/group matches, unchanged spline-index ordering, recomputes each canonical payload SHA-256 from metadata and coefficient bytes, and verifies per-span index validity and 18 admitted members. `period-candidate-manifest-03.json` records every member's source dataset/content ID, provisional rotational family and quarter-turn step, admitted spans and plane-touch box counts. It keeps all 18 selected images across twelve families; families 0–3 and 10–11 each require two images. In particular `coil_1009` and `coil_1010` each have nine admitted boxes and no box wholly inside the closed quadrant, so a one-coil-per-family or centroid-only selection would lose seam geometry.
- All 30 excluded members have a **minimum 54.658837326 cm whole-box separation** from the closed first quadrant; the frozen diagnostic still excludes them after an extra 10 cm displacement stress. This is ample geometric margin on the 1 cm diagnostic winding-pack representation, but the script does not prove a directed enclosure of every floating surface evaluation. Maximum centerline control mismatch between inferred 90-degree rotational successors remains 0.0002030704 cm, so the rotational-family label is provisional rather than exact physical-image equivalence.
- The same manifest builder accepted the separately frame-repaired HDF5 in `period-candidate-manifest-frame-02.json` and independently verified all 48 of its payload hashes. The hash-bound `period-manifest-comparison-02.json` finds **zero** differences in all 48 member/family mappings, all per-span admission lists and plane-touch counts. All 48 content IDs changed, correctly reflecting a distinct coefficient payload; this comparison does not prove geometry or seam equality. `qualification/verify_period_candidate_selectors.py` then constructed actual OpenMC Python surfaces using each selected HDF5 group and canonical content ID, round-tripped XML identity and checked finite broad bounds: **18/18** on the original input (`period-selector-check-02/receipt.json`) and **18/18** on the frame candidate (`period-selector-check-frame-02/receipt.json`). These are representation checks, not particle transport.
- The local data-build audit is `local-cont-20260925/build-audit-20260925-11.txt`: refreshed RAM/C:/D:/process observations before each no-overwrite data output, existing Python/h5py/NumPy/OpenMC environments, caches and source checkouts, planned external acquisition 0 B, under-1-MiB JSON outputs and exact rollback. The manifests do not trim whole periodic coil surfaces, choose periodic-plane normal/ownership, validate winding-pack fidelity, clear unresolved C++ roots or run one-period transport. The separate UQ task was observed active and in progress; no accepted bounded plasma-source output was obtained in this continuation.

## Rectangle-only cutoff-gap localization

- The prior compiled-power prefix experiment needed the experimental cosine/sine unit-circle allowance to exclude member 3's two periodic seam spans. The new one-CPU rectangle-only sweep holds cosine and sine independently in `[-1, 1]` and moves the cutoff from lead minus `1e-5` cm through lead minus **1 cm**. Its final hash-bound `cpp-prefix-rectangle-gap-02/receipt.json` keeps member 2 at **256/256** excluded, while member 3 stays at **382/384** for every gap. The undecided spans are exactly 0 and 383. For the 1 cm cutoff, each leaves one undecided depth-36 tile after 73 visits; span 0 ends with an interval projection determinant containing zero, while span 383 ends with a projected root possibility. The other 36 leaves per seam span exclude by projection. Thus a wider prefix gap alone does not remove the need to represent cosine/sine correlation on this fixture.
- The new receipt validates all 640 unique span rows, six monotone gap results per span, summary totals and known-root controls. All 643 rows' prior fields match `cpp-prefix-padded-03/stdout.jsonl` exactly after removing the newly added fields. The current-computer build audit is `build-audit-20260925-12.txt`: existing GCC/HDF5/Python/cache/checkouts and native library sufficed, no software acquisition or production rebuild occurred, and both small experimental executables were built with one GCC job and `-O2 -fno-fast-math -ffp-contract=off`.
- This result localizes the next arithmetic task; it is not a proof of an absent prefix root. The interval model still needs a justified enclosure of the production floating path, including runtime trigonometry, and the finite candidate root tiles still need existence, uniqueness and ordering proofs. Both shaped-member native queries remain terminal unresolved; there is no newly qualified one-period transport or speed result.

## Correlated alpha-arc feasibility check

- To test a route around the experimental unit-circle allowance, the off-production compiled-power probe now covers the full cross-section angle with correlated cosine/sine arc rectangles. It pads each trigonometric endpoint by `1e-6`, projects the rectangles through the ray equations, contracts them with both transverse equations when interval division is safe, and checks the axial prefix. Counts of 64, 256 and **4,096** arcs were run at lead minus `1e-5` cm with the existing projected-residual padding. The final hash-bound `cpp-prefix-alpha-arcs-03/receipt.json` reports member 2 **256/256** excluded but member 3 only **382/384** at every resolution; spans 0 and 383 remain undecided. Both members retain undecided exact-lead and lead-plus-4-cm controls. All 643 prior output rows' existing fields match `cpp-prefix-rectangle-gap-02/stdout.jsonl` after ignoring the added arc fields and appended outcome slot.
- The first broad arc test and a second interval-contracted version gave the same seam result at 256 arcs; their superseded untracked receipts were removed. The final source, binary, native library, HDF5 and runner are bound in the retained receipt. `build-audit-20260925-13.txt` records fresh RAM/disk/process and existing local toolchain/environment/cache/checkouts before each of three one-job experimental builds, zero software acquisition and exact rollback. No production solver or native library changed.
- This arc-box method has **not** replaced the unit-circle assumption. Its `1e-6` trigonometric endpoint pad is unproved for the production libm path, and even with that pad the tested arc widths leave possible seam intersections. More subdivisions alone have no demonstrated cost-effective route to a proof. The next root step must examine the remaining interval dependency or use a different validated local exclusion/isolation argument, then prove finite root-tile existence and ordering before any native result can be accepted. Terminal unresolved, strict-bank, one-period transport and matched-speed gates remain open.

## Faithful swept-spline default and explicit torus surrogate

- The swept constructor previously sampled a centerline at finitely many angles, accepted deviations up to `1e-6 * characteristic_length`, and silently replaced the payload with an analytic circular torus. That changes the represented coefficient surface for near-torus generic inputs. Even the qualified analytic circle's cubic centerline differs from the ideal 500 cm circle by about **4.7256765e-7 cm** at span midpoints. The runtime now defaults to the **faithful spline** for both single surfaces and coil sets and skips the torus-detection sample loop in that mode. The former shortcut is explicitly named `approximate_torus_surrogate` and is used only by analytic control tests that request it; native HDF5 imports do not opt in. The true constant-coefficient `PeriodicSplineSurface` torus specialization is separate and unchanged.
- A controlled near-torus payload perturbs one major and minor radius coefficient by `2e-6 cm`, within the old sampled torus-detection tolerance. Its faithful and surrogate surface values differ by more than `5e-7 cm` at a boundary point. The new regression checks that the default single surface and default one-member set retain the faithful value, while the explicitly selected surrogate still supports legacy analytic control comparisons. The one-job local compiled-surface target built, and focused CTest **1/1 PASS** after the final ABI-preserving overloads.
- The new mode initially changed the C++ constructor symbols used by existing helpers. Forwarding overloads now preserve the prior one/two-argument symbols while routing their behavior to faithful splines. The one-job native `libopenmc.so` rebuild passed; the retained older `native_adapter_smoke` executable linked and ran against the final library SHA-256 `e05059f2d9ba28ced1e1fe82b39ebaedb3855cc394b716b0272d30af9a4ffe0a`. The hash-bound `native-adapter-faithful-04/receipt.json` shows real OpenMC registration of the faithful analytic circular spline and a **25.000000000000078 cm** distance (fixture tolerance `1e-6 cm`). Both shaped members still report terminal unresolved, both native member adapters throw, and the collection remains blocked. No particle transport ran.
- `build-audit-20260925-14.txt` records current RAM, C:/D: free space, significant processes, existing compiler/HDF5/Python/cache/checkouts, one-job builds, zero software acquisition and rollback. The original native smoke verifier's exact `<=25 cm` check was too brittle for the faithful spline; it now checks finite distance within the stated fixture tolerance and binds its own source hash. Superseded untracked receipts were removed. This correction preserves coefficient fidelity but does **not** clear root completeness, floating bounds, physical WISTELL assembly, generic-import campaign, one-period transport or matched performance gates.

## Align accepted swept roots with implicit classification

- The general swept Newton path accepts a candidate when the parametric surface's **projected** ray residual is small. OpenMC point classification instead calls the spline's nearest-centerline **implicit** `evaluate()`. The final root path now checks that implicit value before returning any otherwise resolved candidate; a nonfinite value or residual above `100 * absolute_f_tolerance` (`1e-8` with default options) changes the disposition to terminal unresolved. Existing earlier-span uncertainty still takes precedence. This is a fail-closed consistency check, not a proof of root completeness or a claim that the finite nearest-centerline search is mathematically exact.
- The diagnostic helper was refreshed against the final native library and reports the current member-2 lead at 16 cm with implicit residual **4.4408920985006262e-16**, and member-3 lead at 17.000000000000227 cm with residual **0**. The slower reference leads have residuals `9.609735229787475e-11` and `6.3855587484340504e-12`. Those values are below the default `1e-8` limit, but both fast leads still carry four/five unresolved earlier intervals and remain blocked. The hash-bound `native-adapter-implicit-02/receipt.json` binds the helper, final `libopenmc.so` SHA-256 `027a5fa6d1ecf89e09ec84cc82cf82b1a3f1e5b2e0fb71e1a69f559ae67b41fd`, analytic HDF5, source and runner. Real native circular-spline registration still returns 25.000000000000078 cm; both shaped member adapters and the collection still throw. No particle transport ran.
- `build-audit-20260925-15.txt` records live RAM, C:/D: free space and significant processes before the one-job local and native builds and helper compile, plus existing GCC/HDF5/Python/cache/checkouts, zero software acquisition and rollback. Focused compiled-surface CTest **1/1 PASS** after the solver edit. The implicit acceptance gate may add an `evaluate()` call to resolved general-swept hits; its throughput impact has not been measured, so no latency or native-ZTorus comparison is claimed. An earlier-domain exclusion argument can now target the implicit ellipse equation without assuming a uniform `sin`/`cos` error for accepted roots, but it still needs a proved floating enclosure and finite root-tile isolation before terminal unresolved can be cleared.

## Reject nonfinite swept input before geometry compilation

- `CompiledSweptSplineSurface` now rejects nonfinite length scales and any nonfinite centerline, frame or radius coefficient before constructing spline evaluators or BVH bounds. Previously a NaN centerline/frame value and an infinite positive radius or length could pass its shape/positivity checks. This guard applies to native HDF5 imports and in-memory generic surfaces. It does not certify finite but degenerate frames or prove that an arbitrary input is geometrically valid.
- Focused constructor regressions inject a NaN centerline value, an infinite frame value, an infinite radius and an infinite sweep length. The one-job standalone target rebuilt and focused CTest **1/1 PASS**. The one-job native `libopenmc.so` rebuilt; its SHA-256 is `d0a9f820b07ba30c4dfa1b5c30ca304b6f7eb7721cb90a24187cbbf303185494`. The hash-bound `native-adapter-finite-01/receipt.json` again verifies native registration and a **25.000000000000078 cm** circular fixture distance within `1e-6 cm`. Both shaped members and the collection remain terminal unresolved, so no transport or performance claim follows.
- `build-audit-20260925-16.txt` records current RAM, C:/D: free space, significant processes, toolchain, Python environment/cache and existing source/build directories before building. Planned software acquisition was **0 B**. The reader still permits explicit non-SHA test identities, as the periodic coefficient schema does; this edit does not promote those identities to a cryptographic payload guarantee. The production WISTELL-D content IDs remain SHA-256 verified by the importer.

## Explicit noncontiguous native collection selector

- The provisional one-period manifest selects **18 of 48** HDF5 coil groups with nonconsecutive IDs. The native swept collection previously accepted only `dataset_prefix` plus a consecutive `dataset_start`/`dataset_count` window, which could not express those 18 candidates without importing unwanted members. It now also accepts an explicit whitespace-separated `dataset_indices` list. The two collection modes are exclusive; indexed input rejects empty, duplicate, negative, nondecimal and unrepresentable IDs before reading any member. Each selected HDF5 group still goes through the existing per-coil content-ID verification and unique member-ID check. Native statepoint HDF5 writes back the exact selected index list.
- The current OpenMC Python `SweptSplineSurface` accepts the same explicit index sequence, retains it in equality, bounds only selected groups, and round-trips it through XML and HDF5. The actual-adapter controlled suite reports **53/53 PASS** in `transport-contracts-indexed-02`; the controlled Python method suite reports **28/28 PASS** in `python-methods-indexed-02`. The real local OpenMC Python API suite reports **17 PASS**. The controlled C++ test double initially failed to compile because it still modeled an older swept constructor signature; it was updated to match the production interface before the successful run.
- The one-job native `libopenmc.so` build succeeded. `native-period-selector-05/receipt.json` binds the final library SHA-256 `5b0d69a3905b296512183a206d326de524876f64a2074a75eb1931c159b67f94`, helper, runner, local Python surface source, manifest and qualified HDF5 hashes. On one CPU it registered the exact 18 manifest member IDs through actual native OpenMC, wrote `dataset_indices` into a statepoint-style HDF5 group, and reimported that group with this checkout's Python `openmc.Surface.from_hdf5`. The native bounding box extends to roughly x/y = **1555 cm**, a reminder that these are untrimmed whole coils and not a closed one-period sector model. The contiguous analytic smoke still registers its circular member at 25.000000000000078 cm and still rejects the shaped collection on terminal unresolved roots (`native-adapter-indexed-01/receipt.json`). No particle transport ran.
- `build-audit-20260925-17.txt` and `build-audit-20260925-18.txt` record live local RAM/disk/processes, existing toolchain/environments/caches/checkouts, bounded one-job builds, zero software acquisition and rollback. An intermediate Python statepoint attempt imported a different installed OpenMC checkout; the final runner pins and verifies this worktree's Python source. The explicit selector closes a representation gap only. It does not prove physical coil-image equivalence, seam ownership, clipping, unique materials, root completeness, floating bounds, generic importer qualification, transport or matched performance. The attempted recursive cleanup of superseded local receipts was rejected by automatic policy; those uncommitted scratch directories remain on disk.

## Centerline-only prefix exclusion feasibility

- A new one-CPU off-production probe works directly from the qualified analytic HDF5 coefficients. It bounds each periodic cubic centerline by subdivided Bézier control hulls and each cross-section by the maximum of four nearby radius controls. On the 550 cm axial diagnostic ray, this broad sphere bound excludes **252/256** member-2 spans and **378/384** member-3 spans before the candidate lead minus 1 cm. Adding the necessary parametric tangent-plane condition excludes **254/256** and **382/384**, leaving seam spans 0/255 and 0/383 respectively. The same four seam spans remain at a 1e-5 cm gap, exact lead and lead plus 4 cm. A frame-independent axial support bound adds no exclusions. These outcomes are in `centerline-prefix-feasibility-04.json`, with both fixture and source hashes.
- A 4097-point diagnostic sample of the oriented ellipse's maximum x on each remaining seam span gives **534 cm** for member 2, at its 16 cm candidate lead, and **557 cm** for member 3, beyond the 550 cm ray origin. Thus an orientation-aware x bound may help the member-2 prefix, while an x-only separation cannot clear member 3; its transverse coordinates and ellipse correlation matter. This sample is not an upper bound. The probe uses ordinary floating arithmetic and rederived B-spline controls, not the production compiled-power path, so none of its exclusions admits a production root. It narrows the next proof work to a floating-audited oriented ellipse/transverse enclosure and finite root-tile isolation on the seam spans.

## Conservative ray-box slab for near-parallel directions

- The shared `BoundingBox::ray_interval` previously treated every direction component with magnitude at most `1e-15` as exactly parallel. That can incorrectly prune a valid distant intersection: a ray from the origin with direction `(1, 1e-16, 0)` reaches a box at x around `1e16` and y from 1 to 2, although the old y-slab test returned no interval. The slab calculation now treats only exact zero as parallel, rejects nonfinite rays, widens finite subtraction and division bounds with `nextafter`, and leaves an axis unconstrained if its arithmetic overflows. The direct regression covers the distant hit, an exactly parallel miss, and a nonfinite origin. It removes this specific false-exclusion mechanism; it does not audit construction of every upstream swept bound or prove root completeness.
- `local-cont-20260925/build-audit-20260925-19.txt` records the current RAM, C:/D: space, significant processes, existing toolchain/environment/caches/checkouts, zero software acquisition, and the bounded one-job builds in the existing local and native build directories. The standalone target rebuilt without warnings and its focused CTest passed **1/1**. The one-job native build passed **10/10** steps and produced `libopenmc.so` SHA-256 `a203e3ff2e447d72ef5b1cf554f6523d8eb9a47a0260d42e11704093d8498f46`.
- Hash-bound `native-adapter-slabs-01/receipt.json` retains the circular fixture's **25.000000000000078 cm** native crossing and the terminal unresolved status of both shaped members and their collection. `native-period-selector-slabs-01/receipt.json` retains exact native registration, statepoint serialization and Python reimport of the 18 provisional whole-coil candidates. Neither receipt represents particle transport. The cost of the wider slab arithmetic has not been benchmarked; no latency or matched native-ZTorus performance claim follows.

## Exact constant-radius fast-path gate

- The swept solver's circular cross-section path used to accept major/minor radius coefficients within `1e-12 * characteristic_length` of the first coefficient. That tolerance can route a varying, otherwise valid coefficient payload through a constant-radius solver. The path now requires every major and minor coefficient to equal the first major coefficient exactly. Constant-coefficient circular coils retain their fast path; varying coils use the general parametric path and its existing terminal-unresolved safeguards.
- A standalone regression sets a valid periodic circular centerline's first major-radius coefficient `5e-5 cm` above the others and sets `characteristic_length` to `1e8 cm`, which the current data contract accepts. The old gate's `1e-4 cm` threshold treated the surface as circular despite a distinct axial crossing. The test requires a candidate within `5e-6 cm` of the cubic spline's `0.7499666667 cm` crossing, distinguishing it from the old constant-radius `0.74995 cm` lead. The one-job standalone rebuild and focused CTest passed **1/1**.
- `local-cont-20260925/build-audit-20260925-20.txt` records the live RAM, C:/D: space, significant processes, existing compiler/environment/caches/checkouts, zero software acquisition and bounded one-job builds. The one-job native library and executable rebuild passed **3/3** steps. `native-adapter-circular-gate-01/receipt.json` binds final `libopenmc.so` SHA-256 `c288013eb24a835cec248544b67dd97ae4fd507d9c66724ae968558e83d9e65e`; actual native registration still returns **25.000000000000078 cm** for the exact circular fixture. The two shaped member queries and collection remain terminal unresolved, and no native particle transport ran. This closes a fast-path fidelity misclassification only; it does not prove roots, floating bounds or throughput.

## Stored-power span enclosure repair

- `build_spans()` formed center/radius bounds from four Bézier controls computed in ordinary floating arithmetic. A deterministic scalar probe (`experiments/probe_swept_bezier_rounding.py`, SHA-256 `7414f496...`; `span-bezier-witness-01.json`, SHA-256 `5cfa2869...`) found a stored-power Horner center **1 cm** below that rounded hull near the endpoint while the tube radius and fixed rounding pad were only 0.25 cm and `1e-12 cm`. The full compiled-surface regression reproduced the failure before the repair: both the centerline and surface-box assertions failed in focused CTest. The diagnostic uses intentionally ill-scaled but finite accepted coefficients; it establishes a real bounding arithmetic defect, not a physical WISTELL-D geometry failure.
- The span compiler now encloses the actual stored-power Horner evaluation over 16 local-parameter tiles using outward-rounded interval products and sums. It includes the rounded local endpoint mapping and computes center/radius extrema from those intervals; box inflation is widened outward. An unbounded intermediate rejects the payload during construction rather than leaving a finite unsound box. The same focused CTest passes **1/1** after the repair. This certifies the polynomial enclosure under finite IEEE arithmetic in the tested path; it does **not** yet bound frame normalization, the trigonometric section, other geometry branches, or root isolation.
- `build-audit-20260925-21.txt` records live RAM, C:/D: space, significant processes, existing compiler/environment/cache/checkouts, zero software acquisition and bounded one-job builds. The native rebuild passed **3/3** steps and yielded `libopenmc.so` SHA-256 `99fc64ba2d975ea8b179030880de41115560c1a25f669db6954814cf6ebee932`. `native-adapter-power-bounds-01/receipt.json` retains the circular crossing at **25.0000000000001 cm** and the same four/five unresolved intervals on analytic shaped members 2/3. `native-period-selector-power-bounds-01/receipt.json` again registers, serializes and Python-reimports exactly 18 provisional WISTELL candidates. Neither receipt ran transport. The effect of the wider boxes on query latency has not been measured, so no performance claim follows.

## Extreme-scale frame normalization repair

- The saved focused audit witness showed that `normalized({2^-537, 2^-538, 0})` produced squared tangent norm **1.25** because the direct sum of squares underflowed, and the reconstructed surface normal reached z = **1.1180339887498949** outside a unit-radius z bound. The new compiled-surface regression reproduced the helper failure before editing the vector arithmetic; it also hit an exception on finite directions scaled by `2^-600` or `2^600`. The shared norm now uses a scaled `hypot` path for magnitudes outside `1e-150..1e150`, with direct component division at extreme normalization scales and when a reciprocal would underflow or overflow. Ordinary-scale vectors retain the prior square-root and reciprocal path.
- The focused CTest passes **1/1** after the repair. It checks unit tangent length and no radius expansion for the saved underflow frame, normalization of finite `2^-600`/`2^600` and smallest-subnormal directions, and physical distance invariance for those two finite scalings through an actual faithful swept-coil distance query. The latter checks a candidate crossing, not an earlier-root certificate. `build-audit-20260925-22.txt` records live RAM, C:/D: space, processes, existing compiler/environment/cache/checkouts, zero software acquisition and bounded one-job builds.
- The one-job native rebuild passed **11/11** steps and produced `libopenmc.so` SHA-256 `3a3dd136201abb937e8940165178b09d681de2acb624daecebb1c5f8eb2cd31b`. `native-adapter-normalization-01/receipt.json` retains the circular crossing at **25.0000000000001 cm** and four/five terminal unresolved shaped intervals. `native-period-selector-normalization-01/receipt.json` retains registration and statepoint/Python round trip for exactly 18 provisional whole-coil candidates. No particle transport ran. The repair removes the demonstrated extreme-scale normalization escape; it does not bound every frame/cross-product rounding path or close root completeness and latency gates.

## Algebraic ellipse seam-prefix experiment

- To avoid the earlier experimental unit-circle relation for `sin`/`cos`, `experiments/probe_implicit_seam.py` samples an algebraic necessary-condition route on the four periodic seam spans of analytic shaped members 2 and 3. At each of 65,537 spline parameters per span, it solves the ellipse-coordinate quadratic along the fixed 550 cm axial ray and records the tangent-plane residual. For a **1 cm gap** before each member's current candidate lead, neither quadratic branch has a sampled nonnegative root in the prefix. At a `1e-5 cm` gap, one branch has many sampled quadratic roots but its smallest sampled tangent-plane residual is about **0.086 cm** (member 2) or **0.080 cm** (member 3); at the exact lead, residuals approach zero. `implicit-seam-sample-01.json` binds the source and analytic HDF5 hashes. These samples are diagnostic, not continuous exclusion bounds.
- The existing off-production compiled-power interval experiment gained a bounded `--quadratic-seams` mode. It interval-evaluates center, frame and radii from the actual stored C++ span powers, forms an interval ellipse-coordinate quadratic for the axial ray, and tests `|F| <= 1e-6` as a stress allowance. The IEEE-nearest/subnormal preflight runs before either mode. The final `quadratic-seam-interval-02/receipt.json` binds the executable SHA-256 `f3921256...`, current native library `3a3dd136...`, analytic HDF5, source, sample and runner. On one CPU, all **four** seam spans exclude the prefix ending **1 cm** before the candidate lead in one interval tile each. All four remain undecided at a `1e-5 cm` gap and at the lead; the four lead-plus-4-cm controls remain undecided. The 16 final rows match the preflight-order predecessor exactly.
- `build-audit-20260925-23.txt` records live RAM, C:/D: space, processes, existing compiler/environment/cache/checkouts and a refresh before the second one-job compile. External acquisition was **0 B**; the experimental executable is separate from production. This result identifies a practical algebraic route to the earlier seam prefix without invoking a libm unit-circle allowance, but it is **not** a production root certificate: the ellipse-coordinate condition and `1e-6` slack are not yet a proved enclosure of the rounded parametric surface and implicit acceptance path. The finite last 1 cm still needs existence, uniqueness and ordering proofs. Native shaped queries remain terminal unresolved; no one-period transport or speed claim is promoted.

## Tangent-plane contraction of algebraic seam roots

- The prior algebraic seam probe left every `lead - 1e-5 cm` prefix undecided because the ellipse-coordinate quadratic alone permits ray points that miss the swept cross-section plane. The off-production interval mode now intersects each quadratic root interval with the ray prefix and evaluates the centerline tangent-plane residual there. A branch is excluded only when that interval remains outside a `1e-4 cm` plane-residual stress allowance; the ellipse-coordinate stress allowance remains `1e-6`. This uses the compiled span powers and the same fail-open interval frame model, without a runtime sine/cosine unit-circle allowance.
- The final hash-bound `quadratic-seam-interval-03/receipt.json` reports **4/4** seam prefixes excluded at `lead - 1e-5 cm` in **19, 35, 19 and 33** interval nodes for member/span 2/0, 2/255, 3/0 and 3/383. All four 1 cm controls remain excluded in one node. At the exact lead and at lead plus 4 cm, all four seams remain undecided under the 10,000-node cap, retaining the known-root controls. The executable SHA-256 is `7fe23d6e...`; the receipt binds the native library, analytic HDF5, source, sample and runner. `build-audit-20260925-24.txt` records current RAM/C:/D:/processes, existing local tools/environments/caches/checkouts, one GCC job and zero software acquisition. No production kernel or native library changed.
- This narrows the seam-prefix proof target from a 1 cm gap to a `1e-5 cm` gap in the **experimental algebraic model**. It does not yet prove that every rounded production parametric hit, or every point accepted by the nearest-centerline implicit classifier, satisfies the two tested equations within those stress allowances. The final `1e-5 cm` tile still needs existence, uniqueness and ordering proofs; the other spans and strict bank need verification under one audited method. Both shaped native member queries remain terminal unresolved, so no particle transport or matched performance claim follows.

## Arbitrary-ray algebraic interval check on frozen torus anchors

- The off-production quadratic interval probe now accepts an arbitrary ray origin and normalized direction. Its ellipse-coordinate polynomial uses the projected ray direction in the two frame axes, and its tangent-plane test evaluates the same ray at each interval root. The previous axial analytic seam mode still passes its 16-row hash-bound check: `quadratic-seam-interval-05/receipt.json` has all four 1 cm and all four `1e-5 cm` prefixes excluded, with exact-lead and lead-plus-4-cm controls undecided.
- A separate mode reconstructs the frozen bank's synthetic torus, verifies its FNV coefficient identifier against each selected bank row, and reads the actual origin/direction fields for anchors a03, a06, a08 and a15. The frozen CSV SHA-256 is `7348483e...`; the one-CPU `quadratic-bank-anchor-02/receipt.json` binds the compiled binary SHA-256 `665e0ae5...`, current native library, bank, source and verifier. Of 22 span/cutoff cases across the two periodic seam spans, only the two a03 cases at **0.00199 cm**, just before its expected **0.002 cm** entry, are excluded. Both a03 spans become undecided at 0.002 cm. The seam/tangent anchors a06, a08 and a15 remain undecided within the 2,000-node cap, including a08 at 1.99999 cm before its ideal tangency. The existing solver itself reports terminal unresolved for all four selected anchor queries; the a03 candidate distance field of 0.502 cm is an unresolved fallback, not an accepted earlier-root result.
- `build-audit-20260925-25.txt` and `build-audit-20260925-26.txt` record fresh RAM, C:/D:/processes, existing toolchain/environments/caches/checkouts before each one-job experimental rebuild, zero software acquisition and rollback. The generalization does **not** establish a global 160-ray exclusion method: the tangent-direction quadratic becomes degenerate near a06/a08, and the interval model still has unproved acceptance-path and floating enclosures. No production library, native transport, or matched performance qualification changed.

## Earlier candidate from unresolved swept spans

- In the production general swept solver, a successful Newton root previously suppressed the safeguarded implicit sign scan of all other unresolved spans. Frozen torus-bank anchor a03 exposed the consequence: the solver reported a **0.502 cm** later candidate while an expected **0.002 cm** entry remained unresolved. The scan now visits every retained unresolved span whose box may start before the current candidate, updating that candidate if an earlier implicit crossing passes the existing authoritative `evaluate()` residual check. It still retains `terminal_unresolved`; a sign scan is not a root-completeness certificate, and a span with a found root may contain another root.
- The compiled-surface regression reproduces a03 from the frozen 64-control torus and requires a candidate within `3e-7 cm` of 0.002 cm **and** `terminal_unresolved=true`. The one-job standalone rebuild passed, and the full standalone CTest passed **2/2**. In the one-CPU 160-ray `earlier-unresolved-bank-02` replay, a03's candidate is **0.001999999999973081 cm** and remains blocked. The campaign has **52 blocked** rows and no accepted nearest-root failures; it is not a pass. Compared with the old `strict-terminal-01` receipt (from an earlier solver revision), a coincident-out row a12 is also blocked, so the receipts are not a controlled before/after classification comparison. A first intermediate replay before removing the fallback's old one-span loop exit is retained separately as `earlier-unresolved-bank-01`; the final scan keeps the same a03 result.
- The one-job native rebuild produced `libopenmc.so` SHA-256 `82edaa5d...`. `native-adapter-earlier-scan-01/receipt.json` verifies that library binding, registers the circular analytic single member at **25.000000000000075 cm**, and still reports terminal unresolved for shaped members 2 and 3 (four and five intervals). Their native adapters and collection remain blocked; no particle transport ran. `build-audit-20260925-27.txt` through `build-audit-20260925-29.txt` record fresh RAM/C:/D:/process and local dependency observations, bounded one-job builds, zero software acquisition and rollback. This change improves a diagnostic candidate while preserving fail-closed behavior; it does not admit any new root, certify the strict bank, or support a speed claim.

## Exact-rotation one-period coefficient candidate

- A sampled 48-member audit of the original WISTELL-D HDF5 found up to **0.49786662 cm** of *hypothetical* minor-axis endpoint shift if its interpolated, unprojected binormal vectors were used directly. At the worst sample the supplied binormal has a **0.0622** tangent component. This is **not** an observed difference between the Python source definition and C++ runtime: both intentionally reconstruct the binormal as tangent cross orthogonalized normal and ignore the interpolated binormal in the evaluated frame. `wistell-binormal-audit-03.json` binds the source HDF5, manifest and auditor and preserves the sampled decomposition; it is neither a continuous fidelity bound nor evidence to change the production frame.
- The original 90-degree rotation inventory has maximum ellipse-tensor control mismatch **51.53995 cm²** because independently seeded elliptical frames use different gauges. The separate frame-repaired candidate lowers that maximum to **1.85843e-5 cm²**, but its centerline rotational copies still differ by up to **0.0002030704 cm**. The new `qualification/build_exact_period_frame_candidate.py` makes a *third, separate* HDF5 candidate: for each of 12 families it copies one canonical coefficient payload into its three other images using only 90-degree coordinate swaps/sign changes, copies radii, records the original content IDs in metadata, and recomputes all 48 canonical content hashes. It never changes the original or frame-repaired HDF5. Against the frame-repaired input, maximum control displacement is **0.0002030704256 cm** for centers and **1.319e-7** for normals; maximum length change is **3.64e-12 cm**. These are coefficient changes, not a bound on full evaluated-surface or physical winding-pack error. `wistell-exact-period-candidate.json` binds the 1,323,520 B output HDF5 SHA-256 `52398769...`, input, manifest and builder.
- Reopening the written HDF5 verified every 90-degree image and content ID. Independent inventory `period-member-exact-01.json` then found **12 closed four-image cycles**, with zero reported centerline alignment and ellipse-tensor mismatch at stored controls. The sector inventory and manifest retained **18/48** candidate members and **3,156** admitted spans; `period-manifest-exact-comparison-01.json` checked all 48 member identities and every admission list against the frame candidate with zero differences and 48 changed content IDs. The local Python selector check passed **18/18**, and `native-period-selector-exact-01/receipt.json` bound the current `libopenmc.so`, registered the exact candidate's 18 selected members through native OpenMC, serialized their explicit IDs and reimported them in this checkout's Python API. These are representation checks: the whole surfaces are still untrimmed, periodic-plane ownership and continuous symmetry under floating evaluation are unproved, source filament/winding-pack fidelity is unqualified, and shaped native roots still block transport. `build-audit-20260925-30.txt` records live local RAM/C:/D:/processes, existing Python/h5py/NumPy/toolchain/caches/checkouts, zero software acquisition and no-overwrite output.
- The separate UQ task still has no admitted bounded source: its `s=0.90` source intersects/touches a fixed-wall facet and the `s=0.80` attempt stopped before mesh construction on the resource-placement guard. A coordination message requested a hash-bound accepted mesh/case/clearance handoff with normalized probabilities and separate physical rate; no provisional or rejected source has been enabled here.

## Sampled period-frame fidelity gap

- `qualification/audit_period_frame_fidelity.py` reconstructs the runtime tangent/orthogonalized-normal/cross-product-binormal frame for the original, frame-repaired and exact-rotation HDF5 payloads. It compares the directional support of corresponding **filled elliptical cross-sections** at four parameters in each of 256 periodic spline spans and 32 in-plane directions per member. The manifest and HDF5 SHA-256 checks bind all three inputs; `period-frame-fidelity-sample-03.json` binds the auditor. These are sampled local-section witnesses, not continuous full-coil surface distances, wall clearances or physical winding-pack tolerances.
- Across 48 corresponding members, the largest sampled section-support change is **1.838929045 cm** from original to frame-repaired, **0.000193184 cm** from frame-repaired to exact, and **1.839015723 cm** from original to exact. The original-to-frame witness is `coil_1010`, span 144 at `u=0.5`, angle 1.767145868 rad. Its centers differ by only `4.02e-14 cm` there, so the 1.84 cm support change comes from the elliptical frame orientation. The earlier coefficient-scale frame repair therefore cannot be described as a negligible evaluated-surface change to the original diagnostic ellipse.
- For each of 12 families, the auditor also rotates the canonical member by one, two or three exact 90-degree turns and compares it with the corresponding stored image, for **36 pairs**. The largest sampled rotated-pair support mismatch is **2.000162633 cm** in the original payload, **0.000193184 cm** in the frame-repaired payload and **0 cm** in the exact candidate. The original witness is `coil_1008` rotated three quarter turns against `coil_1014`, span 28 at `u=0.5`, angle π/2; its centers differ by only `0.000176874 cm`. Zero for the exact candidate is a control on the auditor and matches its algebraic coefficient construction, not a floating-runtime seam or transport certificate.
- The original MAKEGRID source fixes filaments, not an accepted physical elliptical winding-pack orientation. The original payload does not supply exact represented quarter-turn closure, and the exact-rotation candidate changes its sampled diagnostic sections by about 1.84 cm. Consequently neither candidate is promoted to an accepted physical one-period model. Establish the winding-pack frame and cross-section provenance or a validated tolerance before selecting the geometry; separately qualify continuous/floating rotation behavior, periodic-plane ownership, root completeness, source clearance, native transport and matched performance.

## Compiled implicit quarter-turn sample

- `qualification/probe_native_period_symmetry.cpp` loads each of the 36 canonical-to-image pairs through the HDF5 reader and constructs the current standalone C++ `CompiledSweptSplineSurface`. It forms canonical section points from the stored span powers using the same tangent/orthogonalized-normal/cross-product frame operations, then compares `evaluate(point)` with `evaluate(rotated_point)` on the paired member. The grid covers span IDs 0, 64, 128 and 255, including parameters near the **spline** seam, four section angles, and radius scales 0.99, 1 and 1.01. There are **2,304 comparisons per scale** across the 36 pairs. `native-period-symmetry-01/receipt.json` binds the binary, existing standalone static library, source, runner, three HDF5/manifest pairs and generated pair list. This does not call the OpenMC adapter or track particles.
- The largest absolute implicit-value difference between rotated pairs at scales 0.99 / 1 / 1.01 is **0.551339 / 0.562533 / 0.573839** for the original payload, **4.362e-5 / 4.406e-5 / 4.450e-5** for the frame-repaired payload, and **4.441e-16 at all three scales** for the exact coefficient candidate. The original has **256** sign disagreements at each of the 0.99 and 1.01 scales; frame-repaired and exact have **zero** there. At scale 1, signs of floating values close to zero have no physical classification margin; their disagreement counts are retained in the receipt only as diagnostic data.
- The exact candidate therefore passes this finite-grid C++ covariance check at a machine-precision scale in implicit value. It is still a sampled result, not a continuous floating enclosure or a proof at the field-period clipping planes. The sampled C++ agreement does not resolve the original-to-candidate 1.84 cm section change, physical winding-pack provenance, closest-root completeness, native OpenMC transport or matched throughput. `build-audit-20260925-31.txt` records current RAM/C:/D:/processes, installed compiler/HDF5/Python/cache/checkouts, one local compilation against the existing static library, zero software acquisition and rollback.

## Multimodal nearest-centerline span repair

- The implicit `evaluate()` classifier first chooses a nearest centerline parameter. Its previous per-span eight-step golden-section bracket assumed a single minimum of squared distance on each cubic span, although that squared distance is degree six and can have multiple interior minima. A finite accepted eight-control fixture in `experiments/probe_nearest_centerline_multiminimum.cpp` reproduces the problem in the **old compiled binary**: at the origin it selected a centerline point at squared distance **0.23890634088800547 cm²**, while a point on span zero at `u=0.08048901989047327` is **0.17279940860022003 cm²** away. `nearest-centerline-witness-01.json` binds the old binary, fixture source and output. This explicit nearer point suffices to disprove the old selection; no global-minimum claim is needed for that witness.
- `evaluate_in_span()` now forms the degree-five stationary polynomial from the stored cubic centerline powers and the query point in `long double`. For a strict positive or negative Bernstein sign of its derivative it handles the single convex minimum or endpoint minimum directly; otherwise it recursively partitions at derivative roots, brackets sign-changing stationary roots, and compares those candidates with both endpoints. The new regression requires the fixture's interior parameter and squared distance. The one-job standalone rebuild and full CTest passed **2/2**. This removes the demonstrated single-bracket miss in the represented polynomial model; it is **not** a directed floating proof of every stationary root, a certificate that BVH lower-bound comparisons cannot prune a closer span, or a proof that the returned implicit value matches every parametric boundary.
- The one-CPU frozen 160-ray replay in `nearest-centerline-bank-02` retains **87 no-hit, 21 hit and 52 terminal-unresolved** dispositions, with no changed disposition or candidate distance above `1e-8 cm` against the earlier scan receipt. It remains `BLOCKED`; this is a functional regression check, not a qualified throughput comparison. The one-job native rebuild produced `libopenmc.so` SHA-256 `17e430819e380ca3440004212d3bd4b90fd5c4afc030bb69704f11f62384af9e`. `native-adapter-nearest-centerline-01/receipt.json` binds it and reports the real native circular registration at **25.000000000000075 cm**, with the same four/five unresolved shaped-member intervals. Both shaped member adapters and the collection remain blocked, and no particle transport ran.
- `build-audit-20260925-32.txt` through `build-audit-20260925-35.txt` record fresh local RAM/C:/D:/process/toolchain/environment/cache/checkouts before the diagnostic, standalone and native one-job builds, zero software acquisition and rollback. The convex shortcut and recursive root grid still require independent floating enclosures and broader arbitrary-input validation. The bank, one-period WISTELL transport, physical winding-pack, bounded source and matched old/native-ZTorus performance gates remain open.

## Outward nearest-centerline BVH pruning bounds

- The nearest-centerline BVH used an ordinary rounded squared point-to-box distance as a **lower** bound and an ordinary rounded candidate distance as its pruning threshold. Neither rounded expression has the required direction. `qualification/audit_nearest_box_rounding.py` compares two finite binary64 one-axis witnesses against exact `Fraction` arithmetic: the old box calculation exceeds its exact squared gap by **8.08e-17 cm²**, and the old candidate calculation falls below its exact squared distance by **8.93e-17 cm²**. `nearest-box-rounding-witness-01.json` binds the auditor and records values. These witnesses establish an arithmetic pruning risk; they do not show a prior real-coil misclassification.
- The span-BVH box distance now rounds subtraction, square and sum **downward** at each step and treats finite overflow as a finite fail-open lower value. The currently selected centerline point gets an **upward** squared-distance bound; pruning uses a strict `box_lower > candidate_upper` comparison, retaining possible ties. Nonfinite query points are rejected, and a query with no finite candidate squared distance throws rather than silently returning span zero. The multimodal fixture regression still selects the interior nearest point and now checks nonfinite-point rejection. This addresses the demonstrated binary64 bound directions for finite coordinates; a complete audit of every upstream centerline box and floating branch is still open.
- The one-job standalone rebuild and full CTest passed **2/2**. The one-CPU frozen 160-ray `nearest-box-bank-01` replay remains **87 no-hit, 21 hit, 52 unresolved**, with no changed disposition or candidate distance above `1e-8 cm` relative to `nearest-centerline-bank-02`; it remains `BLOCKED`. The one-job native rebuild produced `libopenmc.so` SHA-256 `ff99faa9ae22f1d41dffc8f83a75e02a7b276324801ba2e16c5e058e28d80405`. `native-adapter-nearest-box-01/receipt.json` retains the actual OpenMC circular registration at **25.000000000000075 cm** and four/five unresolved shaped-member intervals; the member adapters and collection still block, with no transport. `build-audit-20260925-36.txt` and `build-audit-20260925-37.txt` record current local capacity/tools/caches/checkouts, zero software acquisition, bounded one-job builds and rollback. No matched old/native-ZTorus latency claim follows from these single replays.

## Bounded plasma source handoff contract

- `python/stellarcsg/plasma_source.py` adds a fail-closed portable consumer for the UQ task's existing VMEC export and native OpenMC sampling fields. Its handoff binds the export result, selected case, source H5M, mesh arrays, sampling result, clearance result and actual fixed-wall H5M by SHA-256 and same-directory basename. It checks class-B one-period identity, positive tetrahedral orientation and volume, profile-integral normalization, conditional spectra and the sampling flags before constructing a unit-strength MOAB `MeshSource`. Physical rate stays `null`; no rate is inferred from source probability. The proposed independent clearance schema and its limitations are specified in `docs/PLASMA_SOURCE_HANDOFF.md`.
- Eight synthetic contract controls pass in the existing WSL OpenMC Python environment, including Python OpenMC object construction and rejection of failed clearance, insufficient margin, failed sampling, non-null rate, bad probability, inverted cell, and tampered source/wall files. The synthetic H5M placeholders are **not MOAB meshes**; these controls neither validate native sampling nor establish clearance or transport. No dependency was acquired or built in this source-interface turn.
- The UQ task's `s=0.90` source remains rejected by fixed-wall intersection/touch and the `s=0.80` attempt stopped before mesh export. A coordination request sent the proposed whole-cell fixed-wall clearance and hash-bound bundle contract to that task. There is no actual accepted source bundle to consume. A clearance JSON assertion alone is not an independent proof; the UQ clearance computation and provenance must be reviewed before source admission. Root completeness, floating enclosure, periodic geometry fidelity, physical winding-pack provenance, native transport and matched performance remain open.

## Endpoint-sphere proxy seed cancellation repair

- The swept-coil solver generates Newton seeds from a capsule proxy. For endpoint spheres it previously used `qb² - 4 qc`, assuming the normalized binary64 ray direction has squared norm exactly one. The preserved finite witness has origin `(2³⁰, 2³⁰, 0)`, direction `normalized(-1,-1,0)`, a unit sphere at the origin, and a line through the sphere center; the old arithmetic gives discriminant **-2048**. Simply inserting the rounded direction squared into the quadratic gives **0** in this witness. `ray_sphere_seed_roots()` now forms the closest-line distance through a long-double cross product, retains the actual direction squared, and generates two ordered seeds. A focused regression checks the central hit and an offset miss. It is a seed generator, not a certified ray-sphere exclusion predicate.
- One-job standalone rebuild and full CTest passed **2/2**. The one-CPU 160-ray frozen bank in `endpoint-proxy-bank-01` remains **87 no-hit, 21 hit, 52 terminal-unresolved**; no disposition or candidate distance changed by more than `1e-8 cm` against `nearest-box-bank-01`. Its exit 2 and measurement-runner exit 1 remain `BLOCKED`; this is a regression check, not a throughput claim. The one-job native rebuild produced `libopenmc.so` SHA-256 `136bbbd5e4c2842a126768d8847bd2fd46fae8b1f249f187abf0f2c041291320`. `native-adapter-endpoint-proxy-01/receipt.json` verifies loader binding and the circular registration at **25.000000000000075 cm**, while shaped members 2 and 3 still have four and five unresolved intervals and the collection fails closed. No transport ran. `build-audit-20260925-38.txt` through `build-audit-20260925-40.txt` record fresh local resources and capabilities before each one-job build, zero software acquisition and rollback.
- This repair removes one known lost-seed mechanism. It does **not** certify every root-capable interval, prove that one solved seed is the earliest root within its span, establish floating proxy/box enclosures, admit the physical WISTELL-D winding pack or enable the one-period transport/performance milestone. The 160-ray bank did not become faster or more qualified from this change.

## Same-span earlier-root disposition repair

- The general swept solver previously marked a candidate span resolved whenever one Newton seed found a parametric crossing. That does not exclude an earlier crossing in the **same span**, even when every other span is excluded. It now retains every ray-intersected candidate span's earlier interval as unresolved until an actual root-exclusion certificate exists; the existing eight-segment implicit scan may improve a diagnostic candidate but cannot clear the terminal flag. The ordinary-entry regression from the 64-control torus retains its ≈0.502 cm lead and requires `terminal_unresolved=true`. This is an intentional fail-closed correction, not a certified-root algorithm.
- One-job standalone rebuild and full CTest passed **2/2**. In the one-CPU frozen 160-ray `same-span-prefix-bank-01`, the prior **21 hit** rows all become **terminal-unresolved**, with unchanged candidate distances within `1e-8 cm`; 87 no-hit rows remain. The bank is `BLOCKED`. The zero accepted general-swept hits are a real loss of usable fast-path coverage, not a pass or throughput result. `build-audit-20260925-41.txt` and `build-audit-20260925-42.txt` record fresh resources, local tools/environments/caches/checkouts, zero software acquisition and one-job builds.
- The one-job native rebuild produced `libopenmc.so` SHA-256 `a64d10088531bda8241e3a5daab710e5714bb5462b18214b9e4bb1021273289f`. The old native smoke probe stopped at the now-unresolved circular member; its `native-adapter-same-span-prefix-01` v3 receipt has state `FAIL` and a **stale positive claim-boundary string**, so that string is invalid. The revised probe continues after circular rejection and the v4 runner emits a state-specific receipt. `native-adapter-same-span-prefix-04/receipt.json` binds the revised probe, runner, native library and analytic HDF5: circular, two shaped members and the collection all reject uncertified nearest-boundary queries; shaped leads remain ≈16 and 17 cm with six unresolved intervals each. No native distance is admitted and no transport ran. `build-audit-20260925-43.txt` and `build-audit-20260925-44.txt` cover the native library and helper builds. The next necessary solver work is to certify or exclude every possibly earlier segment, including a solved span's prefix, then revisit the 21 blocked hits and the floating bounds before timing.

## Periodic seam root-tile conditioning

- The existing off-production compiled-power quadratic probe excludes the two shaped analytic members' ray prefixes up to `lead - 1e-5 cm` but cannot certify the finite root tiles. `qualification/probe_swept_root_tile.py` now diagnoses the two necessary equations in those tiles: the ellipse implicit value equals zero and the ray point is stationary against the centerline tangent. It reconstructs powers from the hash-bound analytic HDF5, takes each unresolved member's lead from the current v4 native receipt, and runs SciPy root plus a finite-difference 2×2 Jacobian in both seam charts. `swept-root-tile-conditioning-01.json` binds the script, HDF5 SHA-256 `39f77da5...` and native receipt.
- Both members yield the same diagnostic distance from their two seam charts: **15.999999999999961 cm** for member 2 and **17.000000000000256 cm** for member 3. The local finite-difference Jacobian determinants are approximately **20.27** and **-11.51**, with 2-norm condition numbers about **993** and **414** in the chosen `(cm,u)` coordinates. The first chart's fitted `u` is about `-2.2e-14` or `-2.0e-14`, just outside `[0,1]`; the paired last chart fits at `u≈1`. A one-chart closed-interval certificate could therefore mis-handle seam ownership. The next root-tile method must cover the periodic seam as a paired domain and deduplicate the physical crossing.
- These float reconstructions, SciPy convergence and finite-difference matrices are **not** interval enclosures, an existence/uniqueness proof, or evidence that the reported candidate is globally nearest. No production solver, native library or dependency environment was changed in this diagnostic turn. All general swept hits and native member queries remain terminal unresolved; one-period transport and performance qualification remain blocked.

## Experimental Krawczyk check of narrow seam boxes

- `qualification/probe_swept_krawczyk.py` applies outward-expanded binary64 interval arithmetic and forward derivatives to the reconstructed two-equation seam model. It reads the preceding conditioning receipt and the same analytic HDF5, then evaluates Krawczyk boxes around both periodic charts of members 2 and 3. `swept-krawczyk-feasibility-01.json` binds both inputs and the probe source. This was a local Python diagnostic; no dependency or native library was changed.
- At a distance half-width of **1e-6 cm** and span-parameter half-width of **1e-7**, both first-span boxes have Krawczyk parameter intervals wholly below zero (member 2: about `[-2.284e-14,-2.203e-14]`; member 3: about `[-2.072e-14,-1.978e-14]`). The paired last-span boxes have Krawczyk images strictly inside their boxes with infinity-norm contraction bounds about **1.36e-7** and **1.21e-7**. The wider `1e-5 cm`/`1e-6` boxes are undecided; three narrower box sizes give the same exclusion/containment pattern. These are feasibility results for the reconstructed real-arithmetic model, near an exceptionally sensitive seam offset of only about `2e-14` in parameter.
- The Krawczyk boxes cover only small neighborhoods of the fitted roots. They do not exclude the remainder of the final `1e-5 cm` lead tile, prove the HDF5-to-compiled floating path or the implicit nearest-centerline acceptance path, or settle periodic seam ownership under rounded C++ evaluation. No root is admitted to the production solver; all general swept queries and native shaped members remain terminal unresolved. Full earlier-root, transport and performance gates remain open.

## Experimental interval cover of the shaped seam lead strip

- The original `qualification/probe_swept_seam_cover.py` partitions a distance strip from the **fitted model root** minus `1e-5 cm` through that root plus `1e-6 cm` across the full local parameter `[0,1]` of the two paired seam spans. It excludes a box only when an outward interval for the ellipse equation or centerline-tangent stationarity equation misses zero. Nonfinite intervals fail open. On the last span, it removes the narrow box already marked as a unique-existence candidate by the preceding Krawczyk report, then covers its complement; the first span has no removed box. The report `swept-seam-cover-feasibility-02.json` binds the script, conditioning result, Krawczyk result and HDF5. **Correction:** that report used the fitted model root, not the earlier native diagnostic lead, as its lower-bound anchor; it left a roughly `2.4e-12 cm` lower-bound gap for member 2 and `8e-13 cm` for member 3 relative to the intended native-lead cutoff.
- With a cap of 2,000 evaluated nodes per chart, the entire tested strip on member 2/span 0 excludes in **1** node, and member 3/span 0 excludes in **759** nodes. The complements of the last-span root boxes exclude in **282** and **484** nodes, respectively. All four runs have **zero unresolved** and **zero nonfinite** nodes. Combined with the narrow Krawczyk boxes, this is evidence for one modeled crossing in each member's last seam chart and none in its first chart over the stated strip. The two necessary equations and the partition are still applied to reconstructed stored powers, not to an enclosure of the compiled C++ path.
- This modeled strip cover does not certify the preceding off-production prefix method's stress allowances, the other spans or bank rays, floating differences in frame and ellipse evaluation, nearest-centerline implicit acceptance, or periodic ownership in the native adapter. It does not alter production code or admit any hit. General swept distance queries, one-period transport and matched performance qualification remain blocked on root and floating certification.

## Binary64 angle spacing at the modeled seam roots

- The general swept Newton path updates a binary64 `angle`, clamps it to a span's angle limits, and evaluates `frame_in_span()` through `(angle - angle_min) * coordinate_scale`. A self-contained GCC 14.2 `-O3` helper reproduces the production expressions for the two analytic shaped last spans; `qualification/audit_seam_angle_quantization.py` binds its source and executable, the production source, analytic HDF5 and preceding Krawczyk report in `seam-angle-quantization-01.json`. `build-audit-20260925-45.txt` records current RAM, C:/D: space, processes, local tools/environments/caches/checkouts, zero software acquisition, one local compiler job and rollback.
- For member 2/span 255, the adjacent predecessor and endpoint angles map to `u=0.9999999999999638` and `u=0.9999999999999999`; the modeled root enclosure is `[0.999999999999975,0.9999999999999774]`. For member 3/span 383, the adjacent angles map to `u=0.9999999999999457` and `u=1`; its root enclosure is `[0.9999999999999833,0.999999999999986]`. Both enclosures are strictly inside a gap between adjacent representable binary64 angles. The last span's constructed `angle_max` equals the two-pi constant for member 2 but is one binary64 step below it for member 3.
- The helper reproduces source arithmetic rather than reading a `CompiledSweptSplineSurface` instance, and the root boxes remain experimental reconstructed-model results. This does **not** prove that the current Newton path misses a tolerance-accepted crossing; nearby representable angles may satisfy its projected residual tolerance. It does show why exact landing on the modeled root is the wrong requirement for a production certificate. A native method must carry a continuous local-parameter interval across this seam gap and enclose its rounded frame/evaluation path before promoting either shaped hit.

## Actual compiled seam-span identity

- `experiments/dump_compiled_seam_spans.cpp` reads the analytic HDF5 through the production coefficient loader, constructs `CompiledSweptSplineSurface` from the existing standalone static library, and emits the public first/last spans for members 2 and 3. `qualification/audit_compiled_seam_spans.py` compares their 128 stored power coefficients bit for bit with the earlier HDF5 reconstruction and checks both last-span angle-limit pairs against the angle-quantization receipt. `compiled-seam-span-identity-01.json` binds the helper executable/source, static library, production source, analytic HDF5 and prior receipt. All **128/128 powers match bitwise**, and both angle-limit pairs match. The existing standalone Ninja target also reports `no work to do` in a read-only dry run.
- `build-audit-20260925-46.txt` records fresh RAM, C:/D: free space, running processes, installed GCC/HDF5/Python/CMake/Ninja, local environments/caches/builds/checkouts, a single local compiler job, zero software acquisition and exact-path rollback. This removes stored-power reconstruction and span endpoint identity as uncertainty for these four analytic spans. The next audit must enclose the rounded `frame_in_span` operations and the native root/implicit acceptance path; this span identity alone neither certifies those operations nor admits a swept hit or transport.

## Native-lead anchored seam cover and residual-band sensitivity

- The seam cover now distinguishes the native candidate (`initial_distance_cm`) from the fitted model root (`candidate_distance_cm`). `swept-seam-cover-native-lower-06.json` starts all four boxes at **native lead minus `1e-5 cm`** and reaches the fitted root plus `1e-6 cm`. With zero residual band, the first charts exclude in **1/775** nodes and the last-chart complements exclude in **282/484** nodes for members 2/3, all with zero unresolved and nonfinite nodes. This corrects the narrow lower-bound gap in the earlier historical receipt. The minimum exclusion margins are recorded per equation and chart; member 3/span 0 has only about **`3.11e-15`** of ellipse margin at its tightest leaf, so zero-band exclusion alone has little floating headroom.
- `swept-seam-earlier-band-07.json` tests an assumed additive residual band of **`1e-12`** for the dimensionless ellipse equation and **`1e-10 cm²/u`** for centerline-tangent stationarity. It partitions the full local parameter range without carving out a root box, over distances from native lead minus `1e-5 cm` through **native lead minus `1e-11 cm`**. The four charts exclude in **57, 2775, 53 and 1669** nodes, respectively, with zero unresolved and nonfinite nodes under a 10,000-node cap. Source hashes, native-bound calculations, dispositions and the nonfinite fail-open predicate were checked. These bands are stress assumptions; no directed error analysis shows that compiled C++ frame or implicit evaluation fits them. They therefore cannot admit a production root or close the other spans and strict bank.

## Sampled compiled implicit seam crossings

- `experiments/probe_compiled_seam_implicit.cpp` queries the actual standalone `CompiledSweptSplineSurface::local_coordinates()` and `evaluate()` path at six binary64 ray distances per shaped member. `qualification/audit_compiled_seam_implicit.py` binds the compiled helper, static library, production source, analytic HDF5, modeled Krawczyk boxes and current native unresolved receipt in `compiled-seam-implicit-samples-01.json`. `build-audit-20260925-47.txt` records fresh local resources, installed tools/environments/caches/checkouts, zero software acquisition, one local compiler job and rollback.
- At the modeled distance-box edges, member 2's compiled implicit values are approximately **`+6.53e-14`** and **`−4.82e-14`**; member 3's are **`−7.57e-14`** and **`+7.59e-14`**. Each member reports identical arc coordinate, center, normal, binormal and radii across its six sampled distances from native lead ±`1e-11 cm` and the model-box points. The model-box midpoint values are about `4.44e-16` and exactly zero, respectively. This strengthens the observed agreement between the continuous parametric model and the compiled implicit evaluator near these crossings.
- Six samples and matching reported frames do **not** prove the nearest-centerline branch stays fixed between them or that the implicit value is continuous there. They also do not provide an interval error bound, certify the other spans or 160-ray bank, or admit a native distance. General swept queries, one-period transport and matched throughput gates remain blocked.

## Uniform last-span stationary-point enclosure

- `qualification/audit_seam_stationarity.py` uses outward binary64 intervals on the real centerline polynomials whose stored powers matched the compiled spans bit for bit. It encloses `G=(ray point−center)·center'` and `∂G/∂u` over every ray distance from **native lead minus `1e-5 cm`** to **fitted model root plus `1e-6 cm`**, with `u∈[1−1e-8,1]` on each shaped member's last span. Exact derivatives of the represented cubic are enclosed by widening the rounded `3×` and `6×` coefficient products. `seam-stationarity-interval-01.json` binds the source, HDF5, conditioning and compiled-span identity receipts.
- On each last span, the interval value of `G` is strictly positive at the tile's left endpoint, strictly negative at `u=1`, and its `u` derivative is strictly negative throughout. Thus for **every** distance in the stated strip the represented real centerline has exactly one local squared-distance minimum in that tile. A mean-value enclosure puts member 2's stationary `u` in **`[0.9999999999999758, 0.9999999999999766]`** and member 3's in **`[0.9999999999999842, 0.9999999999999853]`**. Under both separate and fused rounding of the source angle expression, every exact root in either enclosure maps to angle bits `0x1.921fb54442d17p+2` (one binary64 step below the two-pi constant). Hash, sign, enclosure and angle-cell checks passed.
- This is a **local real-polynomial** result. It does not prove the compiled long-double nearest-root grid attains that local minimum, that no other span is globally closer, or that its rounded frame stays fixed. Those are necessary before the sampled implicit sign bracket becomes a continuous native crossing certificate. The floating error bands and earlier-root proof for all spans also remain open; no production hit or transport is admitted.

## Global nearest span for the represented analytic centerlines

- `qualification/audit_seam_global_nearest.py` bounds squared ray-point-to-centerline distance over **every** span for each shaped member and every ray distance in the corrected seam strip. An upper bound from the last span's `u=1` endpoint eliminates all but spans `0/255` for member 2 and `0/383` for member 3 in **766/894** total interval nodes. The smallest exclusion margin for any other span is about **136.45/61.39 cm²**, respectively. The script then proves centerline stationarity is negative across the full first span (squared distance increases from `u=0`) and positive across the last span until the already certified near-`u=1` minimum (squared distance decreases toward it). Those sign covers use **1/83** and **31/45** nodes, respectively, with no unresolved tiles.
- Exact `Fraction` arithmetic on the stored binary64 powers compares the two seam endpoints for every distance in each strip: last minus first squared distance lies in approximately **`[-3.979042e-13, -3.979039e-13] cm²`** for member 2 and **`[-6.821211e-13, -6.821205e-13] cm²`** for member 3. Each exact upper endpoint is strictly negative. Thus the last-span interior minimum is globally nearest for the **represented real centerline** over both strips. `seam-global-nearest-model-01.json` binds the source, HDF5, stationary-root and compiled seam-span receipts and records the survivor sets, margins and exact rational upper comparisons.
- The first/last powers were checked against compiled spans bit for bit; powers for the other spans were reconstructed from HDF5 and are not yet exhaustively compared with the compiled instance. The proof also does not certify the compiled long-double stationary-root grid, BVH pruning or rounded frame/evaluate path. It narrows the native global-selection gate to those implementation details, but no native hit, one-period transport or matched throughput is promoted.

## Exhaustive analytic compiled-span identity

- `experiments/dump_compiled_all_spans.cpp` reads both shaped analytic members through the production HDF5 loader and emits all public compiled span powers and angle limits. `qualification/audit_all_compiled_span_powers.py` checks every emitted binary64 value against the HDF5 reconstruction, with signed-zero-sensitive bit comparisons. The hash-bound `compiled-all-spans-01.receipt.json` and 435,563-byte `compiled-all-spans-01.jsonl` retain **640 unique spans, 20,480/20,480 matching power coefficients, 1,280/1,280 matching angle limits, and zero mismatches**. The raw dump, row coverage, counts and source/executable/library/HDF5 hashes were independently checked. A local `.gitattributes` pins LF checkout for the raw JSONL so its recorded byte hash survives Windows Git checkout. The existing standalone Ninja target reports `no work to do` in a read-only dry run.
- `build-audit-20260925-48.txt` records fresh RAM, C:/D: free space, significant processes, installed compiler/HDF5/Python/CMake/Ninja, local environments/caches/checkouts, zero software acquisition, one local compiler job and exact-path rollback. Together with the preceding global-nearest model receipt, this closes its **all-span stored-power identity** caveat for these two analytic members. It does not yet certify the compiled nearest-root grid, BVH pruning, floating frame/implicit evaluation, arbitrary payloads or physical WISTELL-D fidelity. Native member queries remain terminal unresolved; no particle transport or matched performance claim follows.

## Seam convexity, rounded ties and compiled branch bound

- `seam-convex-branch-01.json` used exact rational arithmetic on the dumped last-span powers and the original seam distance strips. Both last spans have strictly positive stationary-derivative Bernstein controls and opposing stationary endpoint signs. This was a real-polynomial branch feasibility result, without accounting for the compiled rounded ray point or long-double arithmetic.
- `seam-rounded-ties-01.json` compares the saved compiled implicit samples with the first-span endpoint center. All **12/12** binary64 squared-distance pairs tie, while exact rational stored-power last-endpoint distances are strictly smaller. The old `local_coordinates()` tie outcome therefore depended on BVH visit order. Commit `43331cf1b` ranks a binary64 tie by the long-double centerline minimum already calculated in `evaluate_in_span()`. The standalone compiled-surface tests passed; both standalone and native libraries were rebuilt, and Ninja dry runs reported no pending work. The new sampled compiled implicit report still has the previous shaped sign brackets and frames. The hash-bound native smoke `native-adapter-tiebreak-01` remains `BLOCKED_SINGLE_AND_COLLECTION_UNRESOLVED`; no native crossing or transport was admitted. Build audits 49 and 50 record local resources, zero software acquisition and the bounded builds.
- `seam-convex-branch-native-query-02.json` strengthens the convexity check over **every rounded binary64 x produced by `550−t`** within both seam strips. It ties the source, current standalone library, prior exhaustive compiled-power receipt, compiler macro snapshot and build cache. GCC 14.2 reports binary80 long double (`__LDBL_MANT_DIG__=64`, epsilon `2^-63`) with `-O3 -DNDEBUG` and no fast-math flag. Exact term sums and deliberately loose operation-count bounds give at most about `6.33e-14`/`3.31e-14` of combined control/branch uncertainty versus minimum real Bernstein controls of `141.826`/`69.010`. The stationary value at `u=1` remains positive by over `3.37e-12`/`1.04e-12`, compared with endpoint arithmetic budgets below `2e-15`/`1.04e-15`. Conditional on the recorded binary80 round-to-nearest source expression order, the compiled last-span convex branch and stationary endpoint bracket are stable for these analytic strips.
- This does **not** enclose the subsequent safeguarded Newton iterations, prove BVH traversal/selection over the continuous strips, bound the rounded frame/implicit evaluator, certify the earlier ray prefix, or generalize to arbitrary imported coils. Shaped native member queries and the collection still reject unresolved distances. The one-period transport, matched performance, fidelity and source-interface gates remain open.

## Safeguarded Newton branch feasibility

- `qualification/probe_seam_newton_iterates.py` follows the source loop's sign and Newton-versus-midpoint guards with exact rational interval arithmetic over the rounded ray-x range and the bitwise matched last-span powers. `seam-newton-interval-probe-02.json` binds its inputs and records the full-strip trace plus **4096** exact query partitions per shaped member. For member 2, all partitions agree on six midpoint fallbacks through `u=0.984375`. For member 3, every partition agrees on three midpoint fallbacks and a first accepted Newton step near `u=0.9999999979592`; the next guard remains undecided because straightforward interval division loses correlation between the stationary value and its derivative. The unsplit strip becomes guard-undecided at iterations 5 and 3, respectively.
- Endpoint and midpoint point traces are diagnostic controls through six iterations. Exact rational Newton fractions grow quickly, so this probe deliberately caps point traces at six iterations. It does not include binary80 rounding, prove the remaining source loop, or authorize a root. The next numerical proof needs an analytic curvature/contraction bound or a correlation-preserving interval representation, with a separate floating-error enclosure; simply increasing uniform query partitions does not close the member-3 guard.

## Real safeguarded Newton contraction across the seam strips

- `qualification/audit_seam_newton_contraction.py` avoids repeated interval division. Exact rational Bernstein controls of the stored stationary polynomial bound its derivative over all local `u∈[0,1]`: **`[141.826,141.905]`** for member 2 and **`[69.0102,69.0322]`** for member 3. Corresponding absolute curvature-to-slope Newton error factors `K/(2m)` are below **`0.000541`** and **`0.000327`**. Endpoint residuals enclose the distance of the unique real stationary root from `u=1` in **`[2.379756e-14,2.381091e-14]`** and **`[1.518959e-14,1.519445e-14]`**, respectively. `seam-newton-contraction-01.json` binds the script, prior stationary/compiled-span/convex-branch receipts and exact rational root-gap strings.
- If no Newton step has been accepted earlier, the dyadic midpoint `u=1−2^-18` is still left of either root and its Newton update is guaranteed inside the bracket. Any earlier accepted step has initial root error at most `K/(8m)`; curvature has one sign throughout that near-root neighborhood (`+0.00566…+0.00569` for member 2 and `−0.002029…−0.002024` for member 3), so subsequent **real** Newton updates remain inside the bracket. After three accepted updates, the error bounds are approximately **`5.25e-26`** and **`1.55e-27`** in local `u`, and both endpoint mappings give the same binary64 angle cell as the stationary-root proof. This establishes a bounded real-arithmetic path through the source loop's safeguard without relying on the failed direct interval guard trace.
- The proof does **not** yet enclose binary80 rounding in each iteration, the long-double distance comparison that picks `best_u`, BVH pruning, the rounded frame/implicit value, or the earlier surface-root prefix. The native adapters remain fail closed and no transport or performance qualification follows.

## Sampled compiled binary80 Newton outputs

- After `build-audit-20260925-51.txt` inventoried current local RAM, C:/D: free space, processes, GCC/HDF5/Python/CMake/Ninja, environments/caches/checkouts and zero software acquisition, `experiments/probe_compiled_seam_newton.cpp` was compiled against the existing standalone library. It copies the source stationary-polynomial, convexity and safeguarded Newton expressions, reads the public compiled powers, and reports the loop parameter and rounded angle along with the actual compiled `local_coordinates()` center. `qualification/audit_compiled_seam_newton_samples.py` probes **39** binary64 ray points per shaped member (33 evenly spaced strip points plus the saved seam samples); `seam-compiled-newton-samples-01.json` binds executable/source/library/HDF5 and model receipt hashes. All 78 helper angles equal `0x1.921fb54442d17p+2`, and each member has one compiled center across the sampled points. Member 2 uses 17–18 loop iterations and 14 midpoint steps; member 3 uses 6–7 iterations and three midpoint steps. Every sampled loop stops on zero **rounded** stationary value.
- `qualification/audit_sampled_binary80_stationarity.py` recovers each 21-digit reported parameter on the exact binary80 `2^-64` grid; the largest print-to-grid difference is below `4.4e-22`. At those recovered parameters, exact rational stored-power stationarity residuals are at most **`1.823e-17`** and **`1.180e-17`** for members 2 and 3. Positive Bernstein slope bounds imply real-root parameter errors below **`1.285e-19`** and **`1.710e-19`**. All 78 exact-root brackets map to the helper's binary64 angle cell. `seam-sampled-true-stationarity-01.json` binds the source/helper, sampled outputs, exhaustive compiled powers and contraction receipt.
- These are sampled checks of a separately compiled copy of the source arithmetic, cross-checked against compiled centers. They do **not** prove the optimized library instructions follow the copied loop for every ray point, enclose unsampled query values, establish BVH selection over a strip, certify the rounded implicit function's continuity, or exclude earlier surface roots. Native members and the collection still reject unresolved distances; no transport or matched throughput claim is made.

## Nearest-center distance upper bound at binary64 underflow

- `point_distance_squared_upper()` widened positive rounded components but returned zero when a positive exact square underflowed to binary64 zero. It now widens zero to the smallest positive binary64 value at each nonnegative arithmetic step. `qualification/audit_nearest_upper_underflow.py` gives an exact rational witness: a one-axis gap of `2^-1074` has exact squared distance `2^-2148`; the former Python reproduction returned zero, while the repaired reproduction returns five minimum subnormals. `nearest-upper-underflow-01.json` binds the audit and production-source hashes. This is a local arithmetic contract repair, not evidence that the old BVH skipped a coil hit.
- The existing standalone compiled-surface tests pass after a one-job rebuild. The one-job native rebuild produced `libopenmc.so` SHA-256 `51acdf0e99ae549dfe6f8017f370c7bbbd83316b473049addf6874a4ff8f400b`; `native-adapter-upper-underflow-01/receipt.json` verifies that the smoke probe loaded this library. Both shaped member queries and the collection still reject unresolved nearest-boundary results, and no transport ran. `build-audit-20260925-52.txt` records fresh local resources, installed tools and environments, zero software acquisition, build scope and rollback.
- The exact witness checks the underflow case through a Python reproduction, not a direct call to the internal C++ helper. It does not prove every compiled BVH box is an enclosure, the complete traversal/selection over the continuous seam strips, the earlier surface-root prefix, or physical and generic-import qualification. One-period transport and matched performance remain open.

## Exact real-curve audit of compiled centerline boxes

- `experiments/dump_compiled_span_boxes.cpp` reads the two shaped analytic members through the existing HDF5 loader and exports every actual compiled centerline box alongside the span powers and angle limits. `qualification/audit_compiled_span_boxes.py` requires 256+384 unique spans, bitwise-identical powers and angle limits versus the prior all-span dump, and checks every box against exact rational interval Horner evaluation of its stored cubic over 16 tiles covering local `u∈[0,1]`. `compiled-span-boxes-01.receipt.json` binds the 341,403-byte raw dump, executable, source, existing standalone library, prior powers and analytic HDF5. All **30,720** axis-tile checks pass. The smallest exact margin, `1e-323 cm`, is on a zero-valued axis widened by the box arithmetic; it is not a useful floating-error budget.
- `build-audit-20260925-53.txt` records fresh RAM, C:/D: space, processes, local compiler/HDF5/Python/environments/caches/checkouts, zero software acquisition and the one-job helper build. This closes continuous **real stored-power centerline** containment for these two analytic members. The audit does not independently enclose the rounded `frame_in_span()` path, verify the internal BVH unions or traversal, prove globally correct nearest-center selection, certify the earlier surface-root prefix, or generalize to physical or arbitrary imported coils. Native queries remain unresolved, and no transport or matched speed result follows.
- `qualification/audit_compiled_box_construction.py` independently replays the source's binary64 outward product/sum operations, including the rounded upper local-u endpoint and 16 overlapping tiles, from the dumped powers and angle limits. All **3,840** lower/upper endpoints exactly match the compiled boxes; `compiled-box-construction-01.receipt.json` binds the new auditor to the raw dump, prior exact receipt and production source. This checks the box construction arithmetic for these analytic members under source-order binary64 semantics. It does not establish which optimized instructions executed at every query or prove BVH traversal and nearest-root selection.

## Native Python swept bounds and optimization-repository provenance

- The native OpenMC Python adapter formerly bounded swept coils by the rounded HDF5 control-point hull plus radius. Rounded cubic-power conversion in the compiled kernel can leave that hull: an eight-control binary64 witness produces a center x-coordinate of **9007199254740987 cm** on a stored span, below its **9007199254740988 cm** minimum control. `openmc/surface.py` now builds each periodic span's bound using the same 16-tile outward interval construction as the compiled surface and unions these boxes. The targeted regression is in `test_native_python_api.py`; the full native Python API file passed **18/18** tests. `native-python-swept-box-parity-01.json` records bitwise agreement for all **3,840** compiled centerline box endpoints of the two shaped analytic members.
- On the provisional exact-rotation 18-member candidate, `period-python-swept-bounds-01.json` records a finite Python box. A separately compiled selected-set helper returned the same **six binary64 root-box endpoints**, checked and hash-bound by `period-swept-bound-parity-01.json`. `build-audit-20260925-54.txt` records fresh local resources, existing compiler/HDF5/Python/environments/caches/checkouts, zero software acquisition, one helper compilation and rollback. This selected set still registers **4,608 whole-coil spans** while its sector manifest admits **3,156**; the other **1,452** have not been clipped. Box parity is a representation check, not one-period material ownership or transport acceptance.
- The user identified `D:/Scratch/stellarator_optimization/wistell-d_data` as the correct WISTELL-D data source. Its `coils.wistell-d` is **1,737,790 bytes, SHA-256 `7748369407d28a70f35b5c4a7c0ab860495a08fd0030002112ea933fe570159b`**, exactly matching this checkout's `dev/stellarcsg/test_data/wistell_d/coils.wistell-d` and the existing reproducibility manifest. The source starts `periods 4` and `begin filament`, fixing the filament input identity. The optimization repository's geometry README explicitly calls its magnets a **continuous layer at the winding surface**; `geometry/constants.py` sets its thickness to **30 cm**, and `geometry/geometry.py` passes that radial layer into ParaStell. Those source files have SHA-256 `61cfb85028d6e4c7eeb4b152df10588182311f9bfdcdff245a5b309f764b5` and `d62122be2a0c3f6284c80c8fa0eb725d2fa6b2c6ecf0fc1b43c9c1597fdc2919`, respectively. The current elliptical coil compiler specifies **10/8 cm** section radii and a computed frame separately. The optimization source establishes the filament and radial-layer provenance, but it contains no filament-specific elliptical winding-pack section frame or tolerance that could select between the original and exact-rotation candidates after their **1.84 cm** sampled section-support difference. Neither ellipse is promoted to an accepted physical coil.

## Fixed-wall identity in the bounded source interface

- The separately active UQ project selected corrected ASC P00 geometry H5M SHA-256 `549c42bf66b290f8f56b6f4d7523940c3b32b9d256d993ea42605f4dddb33e39`, with ASC case SHA-256 `742ef72f1e1c07f958ecc3e47796709d1b51a53407cb89b0448f7a595d722c59`. Its September 25 source-stage receipt says the class-B `s=0.80` source mesh and clearance have **not executed**. The staged `check_asc_p00_source_clearance.py` is explicitly a stored-facet diagnostic; even on a pass it leaves `continuous_surface_qualified` and `source_wall_containment_qualified` false. It cannot emit the qualified clearance record that our handoff requires. No source is admitted from this stage.
- `load_bounded_plasma_source()` now requires `expected_wall_sha256` from the caller's fixed transport geometry and compares it with the bundle's hashed wall before returning a source. `BoundedPlasmaSource` carries the wall hash for model assembly. This prevents a self-consistent source/clearance bundle for a different wall from being loaded silently. The existing WSL OpenMC environment passed **9/9** source contract tests, including a caller-wall mismatch control. Windows system Python could not import this checkout's OpenMC because it lacks `endf`; no environment was changed. The synthetic positive fixture still does not establish real source clearance, and no one-period transport was run.

## Provisional sector region and periodic-plane XML

- `qualification/stage_period_region.py` stages two **unfilled** cells from the hash-bound exact-rotation candidate: the selected 18-coil interior and its complement are both intersected with `x>=0`, `y>=0`, a 2500 cm outer cylinder and ±700 cm axial vacuum planes. The x=0 and y=0 planes are paired as 90-degree rotational periodic surfaces. The provisional sector-cell regions serialize as `901 902 -904 905 -906 -903` and `901 902 -904 905 -906 903`. Python XML reimport verified both cell regions, six surface IDs, the reciprocal periodic-plane pair and the exact noncontiguous 18-member selector. `provisional-period-region-01/receipt.json` binds builder, HDF5, manifest, prior Python bounds and generated XML. `build-audit-20260925-55.txt` records fresh local resource/dependency observations, 0 B software acquisition and the no-overwrite staging target.
- Region intersection supplies an explicit sector ownership expression for the whole surfaces. It does not cull the **1,452** registered spans outside the manifest-admitted subset from native evaluation. The cells have no physical material, and the exact-rotation ellipse remains unqualified against a winding-pack source. C++ swept roots, plasma clearance and material transport have not passed; the staged XML is not a usable physical simulation.
- The existing `libopenmc.so` then parsed that same XML through native `read_geometry_xml()` in `experiments/probe_native_period_region.cpp`. The helper verified **two cells, six surfaces, positive x/y sector senses in both cells, opposite coil senses, the swept selector and reciprocal `RotationalPeriodicBC` objects**. `native-period-region-01/receipt.json` binds the helper executable/source, native library, staged XML, Python receipt and raw output, including the observed `libopenmc.so` loader path. `build-audit-20260925-56.txt` records fresh local RAM/disks/processes/tools/environments/caches/checkouts, zero software acquisition, one helper compile and rollback. An initial compile missed the already-local fmt header; the corrected include path succeeded without acquiring a dependency. This is native initialization only: no particle crossed a plane, no swept root was admitted, and no material or source was transported.

## Native periodic-particle control

- `experiments/probe_native_period_particle.cpp` uses the staged two-cell sector and the same existing `libopenmc.so` to move one deterministic particle from x=0 to y=0 and one in the reverse direction at radius **2300 cm**. This radius is outside the selected coil collection's conservative x/y box, so the controls isolate the 90-degree periodic boundary and cell handoff. Both native `RotationalPeriodicBC::handle_particle()` calls rotate position/direction as expected, set the receiving positive-side surface token, preserve unit weight and locate the complement cell `1002`. `native-period-particle-01/receipt.json` binds the source/executable/library/geometry and raw output; `audit_native_period_particle.py` checked the actual `libopenmc.so` loader binding and both crossing results. No history transport ran.
- The first standalone probe crashed because it constructed a particle directly on a boundary without locating it in an interior cell. The next run rejected entry to a void cell because the minimal harness omitted `finalize_cell_densities()`. The final helper now follows the relevant production initializer order (`read_geometry_xml()`, `finalize_geometry()`, `finalize_cell_densities()`), locates each particle from an interior point, then moves it to the plane for the control. `build-audit-20260925-57.txt` through `-59.txt` record fresh resources, installed tools/environments/caches/checkouts and each bounded local build, all with **0 B** software acquisition. These were probe setup corrections, not evidence of a swept-coil or production periodic-BC defect. The controls do not exercise coil crossings, root completeness, material transport or physical source clearance.

## WISTELL-D nominal coil-section provenance

- Following the user's confirmation that `D:/Scratch/stellarator_optimization/wistell-d_data` is the correct source, `qualification/audit_wistell_pack_provenance.py` reproduced the byte-for-byte filament identity across that repository, this checkout and the corrected ASC P00 receipt. `wistell-pack-provenance-01.json` binds all three to SHA-256 `7748369407d28a70f35b5c4a7c0ab860495a08fd0030002112ea933fe570159b` and binds P00's actual configuration to the receipt's SHA-256 `d0896492d28c01f522b96be63a616a645d6d4051dd7947de76078e9906db1752`.
- P00's `magnet_coils` configuration requests **30 cm width, 30 cm thickness, `sample_mod=6`, 90° toroidal extent**. The local ParaStell `magnet_coils.py` (commit `d480424e53747691075d6b0b074face03efd311f`, source SHA-256 `8fa7d1f75c6bcf9bbe906ccabe1808441ce506df865a4effef03c45bbfa3718a`) constructs nominal rectangular corners in a frame formed by projecting the centerline-to-filament-center-of-mass direction onto the tangent-normal plane. It fits CadQuery spline edges through the sampled corners and connects the edges with ruled faces. The optimization code's separate 30 cm **continuous magnet layer** remains a radial-build component; it is not the coil section.
- A 30 × 30 cm point section has area **900 cm²**, versus **251.33 cm²** for the current 10 × 8 cm diagnostic ellipse. If they share center and tangent plane at a station, the rectangle's support is at least **15 cm** in every in-plane direction, while the ellipse's is at most **10 cm**; the support gap is therefore at least **5 cm** irrespective of their in-plane frame angle. This is a nominal section comparison, not a continuous CAD distance bound. The local P00 case directory contains no STEP files, and the exact ParaStell revision that generated accepted P00 CAD has not been tied to this local checkout. The ellipse remains provisional; one-period physical coil geometry and transport are unqualified.

## P00 pre-CAD rectangular edge controls

- `qualification/extract_p00_edge_controls.py` now reproduces the local ParaStell numerical filament filter, centered-difference tangents, center-of-mass-projected normal and four 30 × 30 cm corner offset curves from the hash-bound WISTELL-D filaments. It checks the current local ParaStell source and P00 configuration against the prior provenance receipt before extracting. The resulting `p00-pre-cad-edge-controls-01.npz` (SHA-256 `3a35367b904dc1135eed00db859fabbdb8099741c68ec45a7b2860325062600b`) contains **18** pre-CAD coil candidates, each with **four closed 65-point edge curves** and sampled tangents. The selected source-index labels exactly match the **18** members in the provisional ellipse sector manifest; this is an identity cross-check, not surface equivalence.
- `p00-pre-cad-edge-controls-01.json` binds the input provenance, manifest and output archive. Across the 18 candidates, the maximum sampled side-length residual is **2.14e-13 cm**, corner mean-to-center residual **2.35e-13 cm**, and tangent-to-corner-offset dot residual **1.92e-13 cm**. An independent comparison with the existing StellarCSG MAKEGRID reader confirmed all **48** raw point arrays equal in binary64 and `periods 4`. The local ParaStell module could not be imported by system Python because `gmsh` is absent; no dependency was acquired. These controls precede CadQuery spline fitting, ruled faces, 90° clipping and zero-volume filtering. They do not establish exact accepted STEP surfaces or a rectangle-capable StellarCSG transport surface.

## Accepted P00 DAGMC facet reference found locally

- A targeted inventory located `D:/2026_DPA/asc_poster_builds_orderfix_2feabb0f_r2/P00_full/dagmc.h5m` at **5,017,612 bytes**, SHA-256 **`549c42bf66b290f8f56b6f4d7523940c3b32b9d256d993ea42605f4dddb33e39`**. This is byte-identical to the corrected ASC P00 H5M named by the independent UQ target. The local `geometry_acceptance_receipt.json` also matches that target's receipt SHA-256 **`3316b08887736e7f75cde613d112221360017b6dda494af96a4b993d64c910ed`** and reports `ACCEPTED`. Local P00 `magnet_boundary_facet_catalog.json` declares **18** magnet groups and **3,348** triangle facets tied to that H5M hash. This supplies an actual accepted meshed reference for future sampled fidelity work, although the accepted STEP solids are not in this local directory.
- `qualification/audit_p00_facet_mapping.py` verifies the H5M, accepted receipt, local facet catalog, local magnet manifest, filament source and pre-CAD edge archive identities. The one-to-one minimum-cost assignment between the 18 preclip edge-control boxes and 18 accepted mesh facet boxes follows their ordinal order; forbidding any assigned pair raises total box-endpoint cost by at least **28.689975 cm**. `p00-accepted-facet-mapping-01.json` records every source member, raw index, volume ID, facet count and box. Large endpoint differences for sector-clipped coils are expected and are not a shape-error measure.
- The local magnet manifest SHA-256 **`22488e49c225952b29b8fd69d38412d929949e7d7678b8c4eb2e98934f9daec5`** differs from the manifest SHA-256 **`d589f609af26e212bd4548253beaea9e147b16b8c3601df7ad27c916c168c0e1`** named in the accepted receipt. The mapping is therefore a source-order diagnostic tied to a local facet catalog and accepted H5M hash, not a claim that the local manifest is the accepted one. The UQ project's separate `public-cad-checkpoint-001` STEP coils are for its public ParaStell example inputs, not this WISTELL-D P00 target. Neither the accepted H5M identity nor the ordinal assignment proves continuous CAD-to-StellarCSG agreement, a correct rectangular CSG surface, or native StellarCSG transport.

## Accepted-mesh coil-side bank and ellipse mismatch

- `qualification/build_p00_facet_side_bank.py` selects one large facet per accepted P00 magnet group whose ±0.2 cm normal offsets remain at least 0.5 cm inside the positive x/y sector. At each selected centroid it places inward/outward probes at **0.05, 0.1 and 0.2 cm**, yielding **108** points. Three non-edge triangle-ray directions check each point against the group's full local facet catalog, for **324** inside/outside parity checks. All pass; the smallest intersected barycentric margin is **0.0004400109**, and the smallest positive ray hit distance is **0.0516124 cm**. `p00-facet-side-bank-01.json` binds the accepted H5M, facet catalog and source-index mapping hashes and stores every probe. This finite catalog check is not an independent H5M triangle readback or a CAD surface proof.
- `qualification/audit_p00_ellipse_mismatch.py` compares the inward mesh probes with the exact-rotation **10 × 8 cm diagnostic ellipse**. It interprets every stored binary64 centerline control and probe coordinate as an exact rational number, encloses each *real* periodic cubic span by its four-control convex hull, expands each coordinate box by exactly **10⁻⁶ cm**, and computes exact squared point-to-box lower distances. For **42 of 54** inward probes, covering **14 of 18** coil groups at all three offsets, every padded centerline span box is farther than the ellipse's maximum **10 cm** cross-section radius. The smallest proved gap is **10.885676 cm**, leaving **0.885676 cm** above that radius. Four coil groups are inconclusive under this broad box test; inconclusive does not mean agreement. `p00-ellipse-mismatch-01.json` binds the finite facet-side bank, exact candidate HDF5, sector manifest and spline basis source. This is a sampled mismatch of represented real geometry; it does not enclose the rounded compiled evaluator or establish continuous CAD fidelity.

## Accepted P00 facet surface and 90-degree seam controls

- The experimental `FacetSetSurface` now imports the hash-bound 3,348-triangle, 18-component accepted P00 payload into both Python and native OpenMC. Native registration agrees with all 108 accepted inside/outside probes. The statepoint-shaped HDF5 selector roundtrips through Python, and a wrong content ID, a one-ULP vertex change and a **hash-valid fully inward-wound** shell are rejected before registration. Each connected native shell must have positive signed volume as well as paired, oppositely oriented edges; Python bounds now checks the same closure/winding conditions and rejects hash-valid degenerate triangles. `native-facet-selector-05/receipt.json` binds the current library, helper, accepted payload and fixture; `p00-facet-python-api-04.json` covers Python XML/HDF5 and staged-sector readback. `native-facet-standalone-01.json` binds the passing standalone tests for outward zero-plane cap recognition, sub-coincidence cap alignment, optional cap-distance delegation, coplanar triangulation-seam crossing and fail-closed true-crease ambiguity.
- `facet-period-region-04/geometry.xml` stages two **unfilled** cells within x≥0, y≥0, a 2500 cm cylinder and ±700 cm axial planes. The x=0 and y=0 planes form a 90-degree rotational periodic pair. Its facet selector explicitly delegates outward-oriented triangles within **2.5×10⁻¹³ cm** of those two zero planes to the periodic planes for native *distance* queries; the full closed facet surface remains in side/parity queries. This cap-alignment allowance is one-quarter of OpenMC's 10⁻¹² cm coincidence tolerance. Native geometry initialization requires the explicit x=0/y=0 reciprocal rotational pair when cap delegation is enabled; `native-facet-period-region-05/receipt.json` includes a negative control with both cap planes unpaired. Without delegation, native first-boundary selection chose the facet cap at **80/80 x-cap** and **31/80 y-cap** centroids, so the original sector had incorrect periodic ownership. After delegation, the same **160** cap-interior first-boundary queries select the periodic planes, and direct native periodic handoffs remain in coil cell 1001. Two complement-particle controls at radius 2300 cm also cross as expected. The receipt binds the updated library, region XML, accepted payload and control helpers. These are deterministic geometry controls, not physical histories.
- The accepted mesh's opposing cap outlines are **not exactly periodic** after 90-degree rotation. `p00-facet-periodic-cap-support-02.json` samples each of 80 boundary edges on each cap at 65 points and finds a maximum directed point-to-opposite-polyline gap of **0.00147331 cm**. At a 0.00001 cm inward offset from each boundary-edge midpoint, **33/80** x-to-y and **42/80** y-to-x points lie outside the paired cap. This establishes actual sampled support mismatch near the seam despite the successful cap-centroid handoffs. It does not bound the complete continuous CAD or calculate exact symmetric-difference area. A transport claim needs a justified seam tolerance or a periodic-consistent mesh, plus non-cap crossings, physical materials/source, particle histories and matched performance. The smooth 10×8 cm ellipse remains inconsistent with the authoritative 30×30 cm nominal pack and is not substituted for this accepted mesh.

## Separate periodic-consistent P00 facet candidate

- `qualification/stage_periodic_p00_facet_candidate.py` derives a **new**, separately identified HDF5 candidate from the immutable accepted P00 payload. The x=0 and y=0 caps each contain 80 triangles and 90 distinct vertices. All 90 nearest-vertex matches are mutual and one-to-one, and all 80 triangles have the same unoriented vertex connectivity after the 90-degree map. The transformer assigns each matched pair a shared binary64 midpoint in the cap plane and updates every occurrence of each original cap vertex. `p00-periodic-facet-candidate-01.json` binds the accepted input, output and transformer; its new content ID is `sha256:2167ba06b820b747143de1a22f4704e67a538cdec217bf7ffa0fd9b418536f4c`.
- Independent `qualification/audit_periodic_p00_facet_candidate.py` confirms identical triangle order and component IDs, movement of only the **180** original cap vertices, and bitwise identical paired cap triangles after rotation. The maximum vertex displacement is **0.0033287735930590413 cm**. Corresponding barycentric points of each original and derived triangle differ by no more than this maximum, so it is also an upper bound on the Hausdorff distance between their piecewise-linear triangle unions. `p00-periodic-facet-candidate-audit-01.json` binds both payloads, both receipts and the independent auditor. `p00-periodic-facet-cap-support-01.json` confirms the exact cap-triangle match; its 65-point-per-edge distance calculation has a **2.55×10⁻¹³ cm** floating arithmetic residual and no sampled near-outline coverage disagreement.
- The derived payload passes the same native **108** P00 reference side probes, HDF5/Python selector roundtrip, wrong-ID/one-ULP/inward-winding rejection, two 2300 cm complement periodic crossings and **160** coil cap-interior handoffs in `native-periodic-facet-selector-01/receipt.json` and `native-periodic-facet-region-01/receipt.json`. `periodic-facet-region-01/geometry.xml` retains two unfilled cells. The candidate is **not** the accepted H5M mesh, a continuous-CAD proof or a physical simulation; non-cap particle crossings, edges/vertices, materials, source clearance, history transport, matched performance and generic-import qualification remain open.

## Adversarial review correction and local particle tracking

- The later `-02` candidate and `-03` audit receipts supersede the `-01` evidence above. A GPT-6 Luna adversarial review found that the prior candidate verifier did not pin its accepted H5M ancestry and used numeric float-tuple equality while claiming a bitwise cap comparison. The builder and independent auditor now pin the accepted P00 receipt, payload, and H5M SHA-256 values, compare projected cap-coordinate **binary64 bits** including signed zero, and compute a strictly outward displacement bound by exact rational arithmetic on the stored binary64 coordinates. `p00-periodic-facet-candidate-02.json` and `p00-periodic-facet-candidate-audit-03.json` pass with the same candidate content ID and **0.0033287735930590413 cm** piecewise-linear Hausdorff upper bound. `p00-periodic-facet-cap-support-02.json` confirms bitwise matching candidate caps; `p00-facet-periodic-cap-support-03.json` reconfirms the accepted caps differ.
- The corrected candidate lineage passes the native registration, negative identity/tamper/winding controls, 108 reference probe sides, and 160 cap-centroid handoffs in `native-periodic-facet-selector-03/receipt.json` and `native-periodic-facet-region-02/receipt.json`. `periodic-facet-region-02/geometry.xml` retains two unfilled sector cells. These receipts bind the new `-02` candidate and exclude the superseded `-01` chain.
- `periodic-facet-tracking-smoke-02/run-receipt.json` records a bounded local OpenMC fixed-source run of **10 void-only histories**. An existing cross-section index was used solely for OpenMC initialization; no material was assigned. One selected track crosses complement→coil, rotates across the periodic plane while remaining in coil cell 1001, later crosses other facet boundaries, and leaks at the outer vacuum surface. The receipt binds the executable, library, cross-section index, XML, statepoint, summary and selected track. This establishes that particular geometry-tracking history, not physical interactions, source clearance, CAD fidelity, all edge/vertex cases, tally uncertainty, matched performance, plasma UQ or generic-import qualification.

## Tracking review and CAD-source follow-up

- A further GPT-6 Luna adversarial review showed that the `-02` tracking auditor treated zero terminal weight as leakage without checking the terminal position. The corrected auditor now requires the final selected state to remain in complement cell 1002 and lie on one of the vacuum enclosure surfaces declared in the staged geometry XML, within 1e-8 cm and inside the other enclosure limits. `periodic-facet-tracking-smoke-04/run-receipt.json` reruns the same bounded local 10-history model and passes; its selected terminal point is on the 2500 cm vacuum cylinder. This supersedes the `-02` tracking receipt. The prior observed HDF5 was also on that cylinder; the correction closes a verifier false-pass path rather than a discovered run failure.
- Local source inventory found no STEP/BRep solids in the accepted `D:/2026_DPA/asc_poster_builds_orderfix_2feabb0f_r2/P00_full` case or its `asc_poster_parametric_cases_20260901_orderfix_2feabb0f/P00` configuration directory, and none in the authoritative `D:/Scratch/stellarator_optimization/wistell-d_data` inputs. The nearby `D:/Scratch/wistell_d_parastell_audit_20260827/builds/rect6/rect6.step` is a **40×50 cm** section from a separate audit, and the `D:/Scratch/parastell_open_benchmarks_20260827_01/wistell_d_full_period_90deg/magnets.step` is a continuous radial layer rather than the P00 filament-specific 30×30 cm coil pack. Neither is an accepted P00 continuous-CAD surrogate. P00's receipt records a 0.0001 cm `FACETING_TOL` setting, while the derived seam repair moves accepted facet vertices by at most 0.0033288 cm; the setting alone is not a proven CAD-to-mesh error bound. A continuous P00 surface comparison therefore remains unverified.
- The candidate's independent audit now reconstructs all 90 reciprocal cap-vertex midpoints from the accepted payload, applies them to every matching original vertex occurrence, and requires the complete derived triangle array to match **bit for bit**. `p00-periodic-facet-candidate-audit-05.json` passes and supersedes the earlier `-03` audit. This closes the previous false-pass path in which a different small, paired cap deformation could satisfy the movement bound while claiming the shared-midpoint transform.

## All-group local facet tracking bank

- `qualification/audit_periodic_facet_ray_bank.py` runs one local, one-particle, void-only OpenMC case for each of the **18** accepted P00 facet side-bank labels, DAGMC volume IDs 8–25, at the 0.2 cm exterior offset. Each source direction points toward its paired interior probe. The auditor pins the accepted H5M and side-bank hashes, the derived candidate payload/content ID, the staged sector receipt, reciprocal x=0/y=0 periodic planes, native library binding and existing cross-section index. It checks statepoint run identity, selected track source position/direction/energy, increasing time, complement→coil first transition near the accepted facet centroid, void material IDs, and a zero-weight terminal state on a declared vacuum boundary.
- `periodic-facet-ray-bank-03/receipt.json` passes **18/18** selected histories, with maximum first-crossing distance from its accepted reference facet centroid **3.2886×10⁻⁵ cm**. All source rays enter the single derived coil-union cell 1001 and later exit to vacuum; the source labels identify the selected accepted facet groups, while the OpenMC track does **not** distinguish individual volume ownership within that union. A separate GPT-6 Luna read-only adversarial review checked all 18 recorded track/statepoint pairs and per-row hashes and found no additional false-pass path within this claim. This bank extends geometry-tracking coverage, but remains void-only and sparse; it does not establish physical transport, continuous CAD fidelity, all edge/vertex cases, source clearance, per-coil material ownership or query throughput.

## Distinct derived facet coil cells

- The native and Python `FacetSetSurface` adapters now accept an optional positive signed-32-bit `component_id` after verifying the **full** facet payload content ID, then compile or bound only triangles in that component. They accept one-sided `x0` or `y0` cap delegation. `stage_facet_component_period_region.py` stages 18 separate unfilled coil cells plus one complement in the 90° sector from the same derived P00 payload. It checks each selected shell against the sector's actual radial cylinder, verifies expected component-local cap counts, and roundtrips the XML. Cell labels are accepted DAGMC **volume IDs 8–25**, not raw source-filament indices; the existing bounding-box assignment maps volume 8 to `coil_1009` and raw `coil_1000` to volume 11, without proving exact CAD identity.
- After the fresh zero-acquisition local build audit `build-audit-20260925-73.txt`, the existing one-job CMake/Ninja tree produced native library SHA-256 `fbc38dedc17e20342a56dd50e2d56f86330238dae00b95ab3e4602864967cba4`. `component-facet-period-region-02/receipt.json` binds the staged model. `component-facet-ray-bank-05/receipt.json` passes **18/18** separate void-only one-particle histories: each selected reference side ray starts in complement cell 2000, first enters its own cell `2000+volume_id`, and eventually terminates on a declared enclosure vacuum boundary. The largest first-crossing distance from its accepted facet centroid is **3.2886×10⁻⁵ cm**. Native summaries carry all 18 selector IDs, reimported through the Python HDF5 surface API. The auditor binds the accepted H5M/side bank, derived payload, region, binary, library, cross-section index, and per-run XML/tracks/statepoint/summary hashes. It verifies HDF5 file types and that statepoint and summary timestamps are ordered within the bounded 30-second run; OpenMC tracks provide no shared run ID, so this does not cryptographically join the three output files.
- A GPT-6 Luna high-reasoning adversarial review confirmed the current payload has one connected outward shell per component and lies inside the radial enclosure, while noting that structural XML roundtrip alone does not prove nonoverlap or native tracking. A separate Luna xhigh review found a concrete parser mismatch: Python accepted XML `component_id="1_6"` as 16, whereas C++ rejected it. Python now requires ASCII decimal digits before integer conversion; a direct regression control rejects underscore, plus sign, whitespace and Unicode digit variants. `component-facet-rejections-03/receipt.json` passes native wrong-cap, absent-component and malformed-selector negative controls with nonzero exits and no generated tracks. A third Luna review found no confirmed false pass in the 18-ray evidence but identified the lack of a common run ID across the three HDF5 outputs; file-type and bounded timestamp checks were added before the `-05` bank rerun. The existing Python environment lacks pytest, so the targeted pytest case could not run; no dependency was installed.
- These are derived **faceted geometry** and sparse void-only tracking controls. They do not prove continuous 30×30 cm CAD fidelity, global cell nonoverlap, physical materials/interactions, plasma-source clearance, generic import, all grazing/edge/vertex paths, or matched old/native ZTorus performance. No physical transport or UQ source handoff is claimed.

## Exact cross-component overlap in accepted P00 facets

- `qualification/audit_p00_facet_component_overlap.py` compares the hash-bound accepted P00 facet payload (itself bitwise matched to the accepted H5M's oriented triangle multisets) and the separately identified periodic candidate. Inclusive triangle AABB tests leave **691** cross-component candidate pairs in the accepted mesh and **711** in the derived mesh. Exact rational separating-axis projections exclude **683** and **703**, respectively. The remaining **eight** triangle pairs have two exact open-edge/open-face intersection endpoints, an exact midpoint strictly inside both open triangle faces, and nonparallel normals. The intersections occur only among triangles of DAGMC volume pairs **15–16** and **17–18**. Their stored triangle vertices are bitwise unchanged by the periodic-cap repair. `p00-facet-component-overlap-02.json` binds the auditor, both payloads and lineage receipts. A GPT-6 Luna xhigh reviewer independently recovered a witness for each pair using signed plane distances, edge interpolation and Gram-matrix barycentrics and found no false-positive path in the exact audit.
- These transverse intersections between closed outward faceted shells establish **local positive-volume overlap** of those represented solids. They do not quantify total overlap volume or prove that continuous CAD overlaps. `stage_facet_component_period_region.py` now requires this overlap receipt and explicitly applies **ascending DAGMC volume ID priority** when constructing *void-only* test cells. The staged `component-facet-period-region-03/geometry.xml` is byte-identical to the earlier `-02` geometry, but its receipt records the known intersections and priority. `component-facet-ray-bank-06/receipt.json` again passes 18/18 one-particle entry tracks, and `component-facet-rejections-04/receipt.json` passes the three negative controls. Those tests establish deterministic sampled tracking under a priority convention; they do not approve that convention for distinct physical materials or per-coil tally ownership. General material transport and per-coil tallies need a justified union/material/priority definition tied to authoritative geometry.

## Accepted common magnet material, bounded local interaction smoke

- Direct HDF5 inspection of accepted P00 `dagmc.h5m` SHA-256 `549c42bf66b290f8f56b6f4d7523940c3b32b9d256d993ea42605f4dddb33e39` shows the `mat:magnets` group contains exactly volume IDs **8–25**. The accepted case's `openmc_smoke_base_receipt.json` binds `model.xml` SHA-256 `afee680e3fe7e1e417bb37e289f2ce775fe16fb8fc11298237ea3d959c864027`; that model assigns all magnet volumes the same material 7 (`magnets`, 51 nuclides, 7.957317449024001 g/cm³). This supports a common-material **union for neutron physics** in the faceted overlap, while per-coil volume/tally attribution still needs an explicit policy. It does not prove the continuous winding-pack solids share the same overlap geometry.
- An existing local FENDL neutron index SHA-256 `218236803b2c4a21b038992af93dacfdfe5c0c0401cbbc57f3ff3a947c63abc7` provides one present neutron file for each of the 51 nuclides, totaling **200,757,944 bytes** already on disk. `qualification/audit_p00_magnet_material_smoke.py` binds the accepted H5M/model/receipt, material group, library, side bank, exact staged region receipt and geometry hashes, native binary and library, then assigns material 7 to all 18 coil cells and leaves the complement void. In `p00-magnet-material-smoke-16-02/receipt.json`, one local fixed 1 MeV neutron from accepted volume 16's exterior probe enters cell **2016** with material ID **7**; the recorded track has two subsequent energy-changing in-material states and exits to void. OpenMC exit code is zero; initialization took **43.556 s**, of which **39.861 s** read cross sections, and transport took **0.00316 s**. Those timings are for one startup/history and are **not** matched query or throughput benchmarks. A GPT-6 Luna high-reasoning reviewer confirmed the material mapping and observed entry; its concern about future substitution of self-consistent region files prompted the exact-region hash pins in the final rerun.
- This is a bounded magnet-material loading and tracking check. OpenMC warned about negative probability-table values for seven Mo isotopes at 300 K; the run succeeded, but these data warnings remain part of the record. It does not establish accepted plasma-source clearance, wall/blanket transport, material-priority effects on per-coil tallies, cross-section validation, statistical tallies, continuous CAD fidelity or the requested ZTorus speed comparison. The separate UQ source remains unadmitted.

## Optimization-repository data scope

- After the user confirmed `D:\Scratch\stellarator_optimization\wistell-d_data` as authoritative WISTELL-D data, local read-only inspection found `coils.wistell-d`, `wout_wistell-d.nc`, `source_mesh.h5m`, and `strengths.npy`. The data README (SHA-256 `61cd83dfc7a96aadf58e89c3a493e06babc51e019363b4d627d16c349d5f0107`) identifies the latter two as a ParaStell neutron-source tetrahedral mesh and ordered source strengths in n/s. Their hashes are `65264e15669d09c43f107c3b43c2af24ffbd15173e3bbd0e990b527bfa0b5322` and `0ed18ab58bcc1e9884bf1b5c8bf19a7b7558ce7afe1869f1a2b01710148af6df`, respectively. They have not been admitted as a moving-plasma, one-period source.
- The optimization geometry README (SHA-256 `bb18184ebcd5583c2a5583233ee57316a7ff25714df5e6fc7fe9e8cfe9ffd18b`) says its **45° half-period** blanket model approximates magnets with a continuous layer at the winding surface. `geometry/constants.py` (SHA-256 `61cfb85028d6e4c7eeb4b152df10588182311f9bfdcddf9ff245a5b309f764b5`) sets `magnet_thickness = 30`; `wistell-d_data/generate_data.py` (SHA-256 `a00518130c20c3adfd08f757b4e2066c2a54528154a8cd468333f91a112446b4`) uses that thickness for first-wall-to-coil separation and blanket offset. These files support the filaments, source data, and a 30 cm **continuous-layer approximation**. They do not specify a distinct per-coil winding-pack section or resolve the known faceted overlap ownership. The source provenance and geometry limitation were sent to the separate UQ task for its clearance gate.

## Sampled material entry across all 18 faceted coils

- `qualification/audit_p00_magnet_material_bank.py` writes an 18-site OpenMC file source from the accepted facet-side controls, samples it with replacement for **360** fixed 1 MeV neutron histories, and tracks every history in one local run. It pins the accepted P00 H5M/model/receipt, side bank, staged 18-cell region, exact referenced facet HDF5 SHA-256/content ID/dataset/component selectors, 51-nuclide cross-section index, and selected native binary/library. The final `p00-magnet-material-bank-02/receipt.json` records **360/360** matching first transitions from void complement cell 2000 into each source's corresponding coil cell 2008–2025 with material ID 7; every volume was sampled **12–30** times, with zero missing or failed track entries. The OpenMC process exited 0. Its 39.842 s initialization included 36.027 s of cross-section loading; 0.37243 s was reported as transport-only time for this one case. These times do not form a matched geometry query or throughput comparison.
- A GPT-6 Luna xhigh read-only review independently checked the source HDF5 entries, all track groups, material/geometry settings, statepoint identity and output hashes. It found that the initial runner did not hash the actual facet HDF5 file referenced by `geometry.xml`; the final runner adds that check and pins the binary/library before the `-02` rerun. The result establishes sampled entry and common-material assignment only. One active batch has no statistical uncertainty estimate, the source is not plasma-born, and the continuous CAD, per-coil overlap ownership, physical clearance, full transport physics, tallies, and native-ZTorus performance gates remain open.

## Common-material union across the known coil overlaps

- `qualification/audit_p00_magnet_union_bank.py` stages one material-7 cell whose region is the Boolean union of all 18 derived facet components, with a void complement and the same one-period sector surfaces. It pins the accepted material-bank receipt and its source/material bytes, the actual facet HDF5 payload/content ID, and the native binary/library. `p00-magnet-union-bank-01/receipt.json` records a local OpenMC exit 0 with **360/360** sampled first entries into union cell 2001, material 7; all 18 labeled source probes were sampled **12–30** times. Those labels are source provenance, not per-coil tally ownership.
- `qualification/audit_p00_magnet_union_equivalence.py` checks the actual priority and union XML surface definitions and material assignments, then exhausts all **262,144** resolved sign assignments of the 18 facet surfaces inside the sector. In every case, the priority partition has exactly one magnet cell if and only if the union cell is occupied. In `p00-magnet-union-equivalence-02.json`, the corresponding 360 priority/union tracked histories have **bitwise-identical** stored positions, directions, energies, times, weights, cell instances and material IDs; their cell IDs differ as expected. This establishes Boolean occupancy equivalence for resolved signs and sampled transport equivalence for the recorded tracks. It does not settle unresolved surface coincidences, continuous winding-pack fidelity, coil-specific tally attribution, plasma-source clearance, wall/blanket transport, statistical uncertainty, or matched throughput.

## Exact rounded-Horner enclosure on analytic seam members

- `qualification/audit_compiled_horner_enclosure.py` pins the prior compiled box-construction receipt and raw dump, then checks every outward binary64 product and sum against exact rational arithmetic on its binary64 operands. Across both analytic shaped members' **640** compiled spans and **10,240** local-parameter tiles, all **30,720** axis-tile covers have no gaps and all **552,960** endpoint containment inequalities pass. For each span, the source-order selected-angle endpoint maps inside the covered local-parameter domain. The final **3,840** axis box endpoints match the compiled dump bit for bit. `compiled-horner-enclosure-03.json` binds the auditor, production source, predecessor receipt and raw dump.
- A GPT-6 Luna xhigh adversarial review found no interval, tile or angle-mapping escape under correctly rounded binary64 source-order assumptions. It caught an understated receipt check count and an incompletely pinned predecessor receipt; both are corrected in the final `-03` audit. The result strengthens the rounded-centerline bounding argument for these two analytic members. It does not prove the optimized binary's instruction order, BVH union/traversal, nearest-center selection, arbitrary coil imports, the earlier surface-root prefix, physical fidelity or performance; shaped native distance queries remain terminal unresolved.
