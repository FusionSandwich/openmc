# OpenMC integration contracts on analytic fixtures

Read `../REMAINING_WORK_DISPATCH.md` and `../CLOUD_METHOD_HANDOFF.md`; follow
resource, isolation and evidence rules. Do not rerun the failing WISTELL-D
case, alter solver mathematics or own the period-import compiler. Own new
tests and narrowly justified adapter fixes on a separate JS/ branch. Begin
with analytic/simple fixtures whose answers do not depend on unresolved coil
roots. Keep experimental StellarCSG OFF by default.

Audit the Python/XML/HDF5/C++ interfaces for finite member IDs, material
association, union distance, classification, normal orientation, coincident
boundaries and tally ownership. Specify deterministic ownership without
relabeling true material overlaps as benign coincidence. Test serialization,
rigid transforms, scaled direction, inside/outside and unsupported geometry.
Ensure unresolved exceptions/status survive the adapter and are never converted
to ordinary infinity/no-hit or silently discarded histories.

Use repository-native test conventions and existing analytic fixtures where
possible. Maintain one surface definition across distance/classification/normal.
Document differences between geometric origin contact and OpenMC tracking's
on-surface suppression; do not change tolerances to make failures disappear.
Provide simple manual Python examples with stable per-member IDs/materials.

When a local pre-existing supported OpenMC runtime and data allow it, a tiny
synthetic distributed-source diagnostic may check per-member tallies and union
closure. Otherwise run dependency-light contract tests and explicitly mark
transport blocked. No new data/library acquisition without the required audit;
no Bateman access, full machine or blanket test. Synthetic success is not
WISTELL-D qualification. Return changed files, tests, commands, hashes and
remaining integration gates. Coordinate through explicit proposed interfaces,
not edits to another lane's working files.
