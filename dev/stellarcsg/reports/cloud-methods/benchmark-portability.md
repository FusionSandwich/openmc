# Portable benchmark and backend verification

Read `../REMAINING_WORK_DISPATCH.md` and `../CLOUD_METHOD_HANDOFF.md`. Own a
portable runner/receipt validator under
`dev/stellarcsg/experiments/benchmark_portability/`, not production math or
another WISTELL-D campaign. Work without depending on the current failing test.
Follow the environment/acquisition and isolation rules before builds.

Remove assumptions about desktop paths in a NEW wrapper, leaving frozen
evidence untouched. Discover compiler/HDF5/GMP/OpenMC from actual environment,
validate arguments and child statuses, preserve hashes and refuse overwrites.
Test with small deterministic mock executables and existing analytic controls.
No need to rebuild every old lane to establish runner correctness. A wrapper
exit zero must never hide a failed child. SKIPPED optional legacy comparison
must not discard a valid exact_reference candidate result.

Define receipts for cold unique-ray bank medians, per-query median/P95/P99,
classification/normal timings, candidate/Newton counts, recovery/fallback count
AND time fractions, allocation/memory and error/blocked counts. Use null for
unavailable metrics. Separate instrumented diagnostics from primary timings.
Validate bank uniqueness, hashes, geometry domains and same-session controls;
do not divide unlike banks into a claimed matched speedup. Include confidence
or repeat variability and account for setup/warmup separately. Implement small
synthetic tests for ratios, quantiles, malformed logs and status accounting.

Prepare read-only runtime identification for ordinary DAGMC versus actual
Double Down/Embree linkage: versions, build flags, linked/static provenance,
binary hashes and an explicit UNVERIFIED state where evidence is insufficient.
A filename or DAGMC enable flag alone is not proof of Double Down. Document
the accepted Bateman workflow provenance from the dispatch, but do NOT SSH or
launch remote work; current live Bateman linkage remains unknown.

Write the eventual matched one-period protocol: qualified geometry/fidelity,
same sources/materials/tallies, frozen H5M and binary hashes, lost-history and
closure gates. Execute it only after an eligible candidate, verified physical
assembly and authorized available runtime exist. Do not download/build mesh
stacks to fill a scoreboard now. Historical ordinary-DAGMC ratios are not
Double Down measurements. Return tools/tests and an honest runnable protocol,
not fabricated comparator results.
